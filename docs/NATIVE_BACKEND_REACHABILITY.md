# Native Backend Reachability Audit

This audit records why a file or dependency is retained or removed from the VaultKind native Windows release. Static dependency warnings alone are not removal evidence because Dagger generation, Java service loading, reflection, vault frontends, and platform integrations can create runtime edges that do not appear as ordinary source references.

## Audited entry point

`NativeBackendMain` is the dedicated release and development entry point used by the WinUI host. It initializes only the shared property/logging bootstrap and `DaggerNativeBackendComponent.create().application()`. The component exposes only `NativeBackendApplication` and includes `NativeBackendModule` plus `CommonsModule`. It does not reference or construct `Cryptomator`, `CryptomatorComponent`, `FxApplicationComponent`, or inherited JavaFX screen subcomponents.

The generated `DaggerNativeBackendComponent` confirms that the native root reaches the protocol bridge, vault registry and list manager, vault creation and operations, recovery-key services, auto-lock, mount selection and mounting, notifications, settings, executors, and shutdown handling. Per-vault legacy JavaFX adapters remain lazy compatibility edges inside `Vault`; their presence means JavaFX libraries and the compiled adapter classes are not yet safe to remove.

The former `Cryptomator --native-backend` branch was unsuitable as a class-trimming root because `Cryptomator` bytecode also references JavaFX `Application`, the legacy Dagger graph, IPC GUI startup, and `FxApplicationComponent`. The dedicated entry point removes that false reachability without deleting the inherited launcher.

## Resource evidence

The pre-audit x64 stage copied `target/classes` wholesale. That included 92 FXML files, eight CSS/font files, and 40 inherited GUI images. Their compiled-source sizes were approximately 262 KB, 1.27 MB, and 1.51 MB respectively.

The native backend has explicit runtime resource requirements:

- `logback-native.xml` configures backend logging.
- `i18n/strings.properties` supplies the retained English engine messages.
- `i18n/4096words_en.txt` supplies recovery-key word encoding.
- `THIRD-PARTY.txt` is copied into release notices.
- `module-info.class` is retained while the Java build remains a single modular source tree.
- Every compiled `org/cryptomator` class is retained at this boundary.

No native-backend-reachable code loads an FXML document, legacy CSS/font, or GUI image. Those presentation resources are consumed by the inherited JavaFX application graph, which the native entry point does not construct. The native WinUI frontend has its own compiled XAML/PRI resources and Assets directory outside `Engine/classes`.

## First verified removal slice

`scripts/build-native-release.ps1` now stages all compiled Java classes and only the explicit backend resources above. It rejects a build if any required resource is missing and rejects a stage if `fxml`, `css`, or `img` appears under `Engine/classes`.

This is packaging-only removal. The inherited JavaFX source/resources remain available to Maven tests and the legacy launcher, and no Java service-loaded provider or platform integration is removed.

## Retained pending further proof

- JavaFX libraries and legacy adapter classes remain because common settings and per-vault compatibility facades still compile against them.
- Mount frontend and integrations JARs remain because providers are discovered dynamically and must be tested on supported Windows provider configurations before trimming.
- macOS/Linux frontend JARs, Maven profiles, distribution files, and GitHub workflows are candidates for later Windows-only cleanup, but are not part of this resource boundary.
- Compiled inherited GUI classes remain until a class-level reachability method accounts for Dagger factories, service descriptors, reflection, and all native protocol commands.

## Class inventory

`scripts/audit-native-backend-classes.ps1` now builds a repeatable class-level inventory from the staged `NativeBackendMain`. It asks the release JDK's `jdeps` for every direct authored-class edge with same-package filtering disabled, then walks that graph from the dedicated entry point. It writes the reachable list, the static-candidate list, and a machine-readable summary under `target/native-backend-reachability`.

The first inventory found 1,095 authored `org.cryptomator` classes: 180 are statically reachable from the dedicated native entry point and 915 are outside that static graph. The 915 candidates occupy 3,415,768 bytes (3.26 MiB) before compression. Most are inherited UI classes (666), followed by legacy launcher classes (131).

This is evidence for review, not blanket permission to remove every candidate. The report deliberately marks `removalAuthorized` false. Dagger-generated/nested implementations, service-loaded platform providers, reflection or serialization-created types, and native-protocol/provider coverage must be accounted for before a candidate is consumed by a packaging filter. A separately reviewed, exact class slice may be authorized only with a fail-closed build guard and its applicable staged-engine coverage.

The first classification found that the entire 30-class UI branch was rooted by a single direction violation: neutral `Constants` and `VaultListManager` imported `MasterkeyFileLoadingStrategy` only to reuse its compile-time `masterkeyfile` scheme string. `MASTERKEY_SCHEME` now lives beside `MASTERKEY_FILENAME` in the neutral constants layer. Native password unlock, legacy key-loading bindings, and the inherited strategy all share that value without an engine-to-GUI import.

After that change, the staged graph contains 145 statically reachable authored classes and 950 static candidates. No authored `org.cryptomator.ui` class or `JavaFXUtil` remains reachable from `NativeBackendMain`. The boundary removed 35 classes and 100,621 bytes from the reachable set because the 30 UI classes had five additional authored dependencies. Source and staged classes are still retained at this audit boundary; this result establishes eligibility for a later packaging slice rather than claiming immediate package savings.

## Runtime retention inventory

The audit also inventories dynamic edges that `jdeps` cannot prove on its own. The current 55 release JARs contain 19 `META-INF/services` descriptors naming 30 provider implementations. JAR contents remain whole at this boundary, so all of those filesystem, cryptography, mount, Windows integration, Jackson, Jetty, and logging providers remain present. This inventory is recorded in generated `target/native-backend-reachability/runtime-retention.json` rather than being used to trim inside third-party JARs.

The authored module descriptor declares three `uses` services and six `provides` groups. The native engine launches with `-cp`, not `--module-path`, so the JVM does not activate those authored module service declarations for `NativeBackendMain`; they remain documented for the inherited modular launcher. This distinction prevents legacy JavaFX service providers from being retained under a false native-runtime assumption.

Only two dynamic-construction sites are reachable in authored native code: settings deserialization and native protocol request deserialization. Their three reviewed authored targets (`SettingsJson`, `VaultSettingsJson`, and `NativeUiProtocol.NativeUiRequest`) are asserted to remain inside the 145-class static closure. The audit also applies a nested-class family check; no nested class belonging to a reachable outer class currently falls into the candidate set. Dagger-generated classes used by the native graph are explicit bytecode dependencies and are already present in the static closure.

The staged-engine behavioral probe now owns an isolated disposable fixture under its temporary profile. It rejects malformed JSON without losing the listener, reconnects, verifies `backend.hello`, confirms an initially empty `vault.list`, and inventories the current mount providers without changing the selection. It then creates and registers a disposable encrypted vault, verifies recovery-key display and wrong-password rejection, changes and recovers the password, renames and removes the vault, reconnects the existing vault, removes it again, and confirms the isolated list is empty. It also verifies `vault_not_found` for all 11 vault-ID commands, checks unsupported-protocol and unknown-operation errors, and requests graceful shutdown. The wrapper removes the entire temporary engine profile and vault fixture afterward. Actual mounting, provider-selection changes, readable-drive operations, and live file activity remain deferred.

The first physical filter removes only the six compiled classes in `org.cryptomator.ui.dialogs`. Every class in that package is an unreachable static candidate and directly belongs to the inherited JavaFX/FXML dialog layer. The release script names every file explicitly and compares the reviewed list with the compiled package before removal; any future class-set change fails the build pending a new audit. This is deliberately not a wildcard or a general license to consume the candidate report.

The second physical filter applies the same guard to the ten compiled classes in `org.cryptomator.ui.wrongfilealert`. That package is also wholly within the unreachable inherited JavaFX/FXML window graph; its main-window references are unreachable from the native entry point. The native vault-health and warning experiences do not use these legacy classes.

The third physical filter applies the guard to the ten compiled classes in `org.cryptomator.ui.updatereminder`. Every reference to this package is inside the unreachable inherited JavaFX application/window graph. This omits only the legacy reminder window and does not remove the update-checking engine or affect the Microsoft Store update path.

The fourth physical filter applies the guard to the ten compiled classes in `org.cryptomator.ui.sharevault`. The package is wholly within the unreachable inherited JavaFX sharing-window graph. VaultKind's native sharing/navigation experience is separate; vault data and engine operations remain packaged.

The fifth physical filter applies the guard to the twelve compiled classes in `org.cryptomator.ui.stats`. They implement the unreachable inherited JavaFX statistics chart window. The underlying vault-statistics engine remains packaged for native use.

The sixth physical filter applies the guard to the eleven compiled classes in `org.cryptomator.ui.decryptname`. They implement the unreachable inherited JavaFX filename-inspection window and its view model. The cryptographic filesystem and vault engine remain packaged.

The seventh physical filter applies the guard to the eleven compiled classes in `org.cryptomator.ui.error`. They implement the unreachable inherited JavaFX error window, its Dagger wiring, and its window-specific discussion model. Native error reporting and the shared `org.cryptomator.common.ErrorCode` engine class remain packaged.

The eighth physical filter applies the guard to the fifteen compiled classes in `org.cryptomator.ui.controls`. They are inherited JavaFX-only labels, icons, text fields, password widgets, and view helpers referenced exclusively by the unreachable legacy JavaFX graph. The native WinUI controls, native password handling, and neutral `org.cryptomator.common.Passphrase` engine class remain packaged.

The ninth physical filter applies the guard to the fifteen compiled classes in `org.cryptomator.ui.quit`. They implement the unreachable inherited JavaFX quit and forced-quit dialogs. VaultKind's native graceful-shutdown protocol, vault lifecycle engine, and native window-close handling remain packaged; the bundled-engine probe exercises the native shutdown path after every release rebuild.

The tenth physical filter applies the guard to the seventeen compiled classes in `org.cryptomator.ui.lock`. They implement the unreachable inherited JavaFX lock, force-retry, and failure-dialog workflow. The package only catches the mount API's unmount-failure type; it neither provides nor dynamically loads a mount service. Neutral `Vault.lock`, every mount-provider integration, and VaultKind's native lock handling remain packaged.

The eleventh physical filter applies the guard to the fourteen compiled classes in `org.cryptomator.ui.changepassword`. They implement the unreachable inherited JavaFX password window, its password-strength controls, and Dagger wiring. Every external caller is also in the unreachable JavaFX graph, and the package is not a service provider. Its controller optionally updates a remembered password through `KeychainManager`, but VaultKind's native frontend does not expose remembered vault passwords and its native password operation never constructs this controller or calls the keychain. The common keychain manager and Windows keychain provider remain packaged for their independently retained uses. The isolated disposable-vault probe directly verifies the native masterkey backup, password change, retired-password rejection, and recovery-key stability paths.

The twelfth physical filter applies the guard to the forty-eight compiled classes in `org.cryptomator.ui.recoverykey`. They implement the unreachable inherited JavaFX recovery wizard, its clipboard/display controllers, validation controls, expert-settings scenes, background tasks, and Dagger wiring. Every class is a static candidate, every external caller belongs to the inherited JavaFX graph, and neither service/provider metadata nor the native component references the package. Cryptographic recovery behavior is not removed: `RecoveryKeyFactory`, `MasterkeyService`, `RecoveryDirectory`, `CryptoFsInitializer`, the English recovery dictionary, and their native callers remain under neutral `org.cryptomator.common.recovery`. The native frontend does not expose the legacy restore-all wizard; its supported recovery-key creation, display, validation, password reset, retired-password rejection, and key-stability behavior is covered by the isolated disposable-vault probe.

The thirteenth physical filter applies the guard to the forty-seven compiled classes in `org.cryptomator.ui.addvaultwizard`. They implement the unreachable inherited JavaFX create/connect wizard, location chooser, password/recovery scenes, readme generator, and Dagger wiring. Every class is a static candidate, every external caller belongs to the inherited JavaFX graph, and the runtime-retention inventory contains no package entry. `NativeVaultCreator` independently creates the directory and masterkey, initializes the encrypted filesystem with the selected shortening policy, optionally generates the recovery key through the retained neutral factory, writes VaultKind's readable and encrypted-storage readmes, and registers or reconnects the vault through `VaultListManager`. The isolated fixture verifies creation, registration, recovery operations, removal, existing-vault reconnection, and final deregistration.

The fourteenth physical filter applies the guard to the twelve compiled classes in `org.cryptomator.ui.forgetpassword`. They implement only the unreachable inherited JavaFX confirmation window that deletes a remembered vault password through `KeychainManager`. All classes and callers belong to the static candidate graph, and the runtime-retention inventory contains no package entry. VaultKind's native frontend does not store remembered vault passwords and never constructs this window. This exclusion does not remove keychain support: all seven compiled classes under `org.cryptomator.common.keychain` and the Windows keychain integration remain packaged for independently retained engine uses.

The fifteenth physical filter applies the guard to the twelve compiled classes in `org.cryptomator.updater`. Their only external source callers are the inherited JavaFX Preferences and update-reminder controllers, and their generated Dagger construction belongs only to `DaggerCryptomatorComponent`'s legacy JavaFX subcomponent. The package is absent from service/provider metadata and has no reflective construction edge. `JavaVaultEngineHost` and the isolated engine probe both launch the native backend with `cryptomator.disableUpdateCheck=true`; the native frontend exposes no updater. Microsoft Store packages receive updates through the Store, while the documented unsigned portable fallback intentionally has no in-app updater until VaultKind owns a dedicated release channel. The retained source remains available for future release-channel work, but its inactive HTTP client, JavaFX scheduled service, fallback browser action, and update-step runner are not staged.

The sixteenth physical filter applies the guard to the twenty-one compiled classes in `org.cryptomator.ui.convertvault`. Despite the package name, this is only the inherited JavaFX wizard for converting a Cryptomator Hub-managed key source into a local password/masterkey vault; it is not the vault-format migration engine. Its only external caller is the unreachable legacy Hub tab under Vault Options, its generated subcomponent exists only inside the legacy JavaFX Dagger graph, and it has no service/provider or reflective retention edge. The native backend explicitly accepts only the local `masterkeyfile` key scheme during password unlock and never constructs this component. Hub configuration and compatibility code, all seventy `org.cryptomator.ui.keyloading.hub` classes, neutral recovery primitives, and the separate vault-format migration package remain staged. The conversion source remains available as a reference if a native escape-from-Hub workflow is designed later.

The seventeenth physical filter applies the guard to the twenty-seven compiled classes in `org.cryptomator.ui.vaultoptions`. They are the unreachable inherited JavaFX Vault Options window, controllers, converters, annotations, and Dagger subcomponent wiring. Every external constructor edge belongs to the legacy JavaFX application/main-window graph; the package has no service/provider or reflective retention entry. The native frontend supplies its own Preferences and Manage Vault workflows and never constructs `VaultOptionsComponent`. This filter does not remove the settings or provider behavior those controllers formerly presented: synchronized `VaultSettingsData`, mount providers, `MountServiceSelector`, `Mounter`, Windows drive discovery, keychain, recovery, health, Hub key loading, and the retained vault-format migration runner remain staged. The disposable-vault probe continues to verify mount-provider inventory and native password/recovery operations.

The eighteenth physical filter applies the guard to the twenty-nine compiled classes in `org.cryptomator.ui.preferences`. They are the inherited JavaFX Preferences window, page controllers, converters, annotations, and Dagger subcomponent wiring. The controllers present settings and invoke services, but do not implement the retained services themselves: global mount-provider discovery and selection, Windows startup registration, keychain providers and management, diagnostics/logging paths, tray service-provider behavior, and the neutral settings model remain packaged. The inactive updater was independently excluded in the fifteenth slice. VaultKind's WinUI frontend owns its Preferences pages, appearance and privacy persistence, diagnostics-folder action, startup toggle, and Virtual Drive provider UI, and never constructs `PreferencesComponent`. The package has no service/provider or reflective retention entry. The full disposable-vault probe continues to inventory mount providers and exercise the native engine after exclusion.

The nineteenth physical filter is a partial exact slice of `org.cryptomator.ui.unlock`. It omits twenty-four inherited JavaFX controllers, scenes, annotations, the JavaFX `Task` workflow, and their Dagger subcomponent wiring, while deliberately retaining `UnlockCancelledException`. The removed workflow is constructed only by the legacy JavaFX application graph; native `vault.unlock` independently validates the local masterkey scheme, loads the key, calls neutral `Vault.unlock`, handles state transitions and errors, and mounts through retained services. `UnlockCancelledException` remains because the retained masterkey, Hub key-loading, and health-check source still references it for inherited compatibility. A dedicated partial-package guard compares the compiled package against the exact union of removed and retained files, verifies every reviewed file in the stage, and removes only the authorized subset. Neutral vault/key-loading/mounting code, Hub support, mount exceptions, providers, and migration remain packaged. The complete disposable-vault probe verifies native password unlock, wrong-password rejection, password/recovery rotation, and shutdown after exclusion.

The twentieth physical filter applies the guard to the eleven compiled classes in `org.cryptomator.ui.notification`. They implement only the inherited JavaFX always-on-top popup for queued `FileIsInUseEvent` messages, paging, dismissal, and the event's ignore action. The component is constructed and polled only by the legacy JavaFX application graph and has no service/provider or reflective retention entry. This exclusion does not remove the event pipeline: reachable `NotificationManager`, `VaultEvent`, and the `NotificationHandler` service interface remain staged, as do vault filesystem-event consumption and provider code. Native VaultKind does not currently expose or poll the queued file-in-use interaction; that is an existing native functional gap to implement deliberately, not behavior supplied by these unreachable JavaFX classes. The disposable-vault probe does not synthesize a live cross-device `FileIsInUseEvent`, so that future native workflow requires dedicated coverage.

The twenty-first physical filter applies the guard to the nineteen compiled classes in `org.cryptomator.ui.eventview`. They implement only the inherited JavaFX event-viewer window, embedded view, list cells, vault filter, context actions, and Dagger wiring. Their external construction edges belong to the legacy JavaFX application and main-window graphs, and the package has no service/provider or reflective retention entry. The controller consumes but does not implement the event pipeline. Reachable `FileSystemEventAggregator`, `FSEventBucket`, `FSEventBucketContent`, `NotificationManager`, `VaultEvent`, vault filesystem-event consumption, the `NotificationHandler` service interface, and reveal-path integrations remain staged. VaultKind's native Activity history and Vault Doctor are independent native features; neither constructs the JavaFX event viewer. Native handling of cryptofs conflict, broken-node, decryption-failure, and file-in-use events remains a documented workflow and coverage gap rather than behavior supplied by this unreachable presentation package.

The twenty-second physical filter is deliberately only an eleven-class partial slice of the fifty-seven compiled classes in `org.cryptomator.ui.health`. It removes the JavaFX check-list cell controller/factory, check-state icon view, and result-list cell factory: rendering helpers whose only purpose is loading FXML list cells or drawing JavaFX status glyphs. The exact removed-plus-retained union is fail-closed. All forty-six functional and workflow classes remain, including `Check`, `CheckExecutor`, `Result`, `ResultFixApplier`, `ReportWriter`, `StartController`, `CheckListController`, `CheckDetailController`, `ResultListCellController`, key-loading compatibility, the health component/module, and their generated factories. Native Vault Doctor remains only a local configuration/status review; this slice does not claim it replaces encrypted-vault diagnostics or repairs.

The twenty-third physical filter is an eight-class partial slice of the fifty-eight compiled classes in `org.cryptomator.ui.mainwindow`. It removes only the empty JavaFX Activity-page navigation controller and the JavaFX vault-list cell controller/factory rendering helpers. The other fifty classes remain, including diagnostic cases/catalog, Assistant presentation, main-window navigation, keyboard accelerators, Dagger wiring, vault selection/context actions, add/connect/recovery/migration flows, and locked/unlocked/error detail controllers. In particular, the scene factory remains because its accelerators initiate vault workflows; static candidacy alone is not enough to discard those actions.

The twenty-fourth physical filter is a thirteen-class partial slice of the thirty-four compiled classes in `org.cryptomator.ui.fxapp`. It removes only the unreachable legacy JavaFX application root, its Dagger component/module and provider factories, and its JavaFX theme/style coordinator. The other twenty-one classes remain: app-launch handling, auto-unlock, quit/shutdown behavior, window/workflow coordination, filesystem-event and notification adapters, JavaFX reveal-path and appearance providers, scope metadata, and the primary-stage qualifier. The authored module still names the retained reveal-path and appearance providers; the filter does not consume those integration-facing implementations.

The twenty-fifth physical filter is an exact 131-class partial slice of the 151 compiled classes in `org.cryptomator.launcher`. It removes the retired `Cryptomator` JavaFX/IPC entry graph, `CryptomatorComponent`/`CryptomatorModule`, file-open/IPC handlers, and the complete generated `DaggerCryptomatorComponent` family. It retains the exact twenty-class native/shared closure: `NativeBackendMain`, `NativeBackendComponent`, `NativeBackendModule`, the complete generated `DaggerNativeBackendComponent` family, `AdminPropertiesFactory`, `EventualLogger`, and native module factories. The runtime inventory identifies no launcher reflection, serialization, or service-provider target; its dynamic targets remain settings/protocol DTOs inside the reachable closure. The staged engine still launches `NativeBackendMain` explicitly and the probe exercises that exact entry point.

The twenty-sixth physical filter is a four-class partial slice of the forty compiled classes in `org.cryptomator.ui.common`. It removes only `FontLoader` (and its exception), whose sole consumer is the already excluded custom JavaFX icon control, and `SystemBarUtil` (and its placement enum), whose sole consumer is the already excluded JavaFX notification popup. The exact removed-plus-retained union is fail-closed. All thirty-six other common classes remain, including `VaultService`, task orchestration, URLs, FXML/stage helpers used by retained compatibility workflows, animations used by retained migration and health code, micro-interaction support, Windows caption support, and `WeakBindings`, which is still consumed by the retained masterkey-file password workflow.

Static candidacy alone remains insufficient. The ten `org.cryptomator.ui.traymenu` classes were reviewed and deliberately retained because the runtime inventory identifies `AwtTrayMenuController` as a `TrayMenuController` service provider. The package is not eligible for this static-only filtering process without stronger runtime evidence.

The twenty-five `org.cryptomator.ui.migration` classes were also reviewed and deliberately retained. This package invokes the real cryptofs `Migrators.migrate(...)` chain, supports consecutive migrations, preserves `NEEDS_MIGRATION` on failure, and handles wrong passwords, missing filesystem capabilities, excessive filename lengths, progress, and continuation events. The native frontend currently exposes the engine's `NEEDS_MIGRATION` state as “Update required” but has no migration protocol command, native migration workflow, or disposable legacy-vault fixture proving equivalent behavior. Removing the only packaged migration runner before those prerequisites exist could strand a user on an older vault format, so static unreachability is not sufficient evidence.

The twenty-seventh physical filter is an exact Windows-only slice of the eleven compiled `org.cryptomator.networking` candidates. It omits only `SSLContextWithMacKeychain`, its two anonymous helper classes, and `SSLContextWithPKCS12TrustStore`. The native engine launches `NativeBackendMain` on the classpath, so the authored module descriptor's service declarations are inventoried but inactive; the excluded legacy `Cryptomator` launcher is the only source caller that selected an `SSLContextProvider`. The seven Windows-relevant classes remain: the provider interface and exception, shared alternate-trust-store base, combined keystore SPI, and complete `SSLContextWithWindowsCertStore` family. This preserves Windows certificate-store and fallback-JDK trust behavior for future Hub HTTPS integration instead of treating static candidacy as permission to remove TLS support.

The twenty-eighth physical filter removes the exact nine-class `org.cryptomator.ipc` package. `Client`, `Server`, `LoopbackCommunicator`, their communicator/listener contracts, and the reveal/launch-argument message framing implement only the inherited JavaFX application's single-instance protocol. Their sole external source callers are the already excluded `Cryptomator` and `IpcMessageHandler` classes; staged bytecode has no inbound IPC edge, and the package has no service/provider or reflective construction entry. VaultKind's native engine uses the retained `NativeUiBridge` with its separate socket path, framed JSON protocol, backend identity handshake, request validation, and shutdown operation. The two socket implementations share only JDK transport primitives and are not interchangeable.

The twenty-ninth physical filter removes the single root `org.cryptomator.JavaFXUtil` class. It is a JavaFX platform-startup helper whose only source callers are two Java unit tests; production source, FXML/resources, reflection sites, service metadata, and retained compatibility workflows do not reference it. The source class remains available to Maven test compilation, while the native release package's exact root-class inventory is fail-closed around this sole compiled file.

The thirtieth physical filter is an exact generated-only slice of `org.cryptomator.nativeui`. It removes nine Dagger factory classes for the native application, terminator, mount settings, UI bridge/protocol, vault creator/operations, and vault-list snapshot provider. Generated `DaggerNativeBackendComponent` bytecode directly invokes the retained constructors and has no edge to these standalone factories; no reflection or service metadata names them. The fail-closed package union retains exactly twenty-one functional native application, bridge, protocol DTO/source, vault operation/result, mount setting/result, snapshot, and termination classes.

The thirty-first physical filter is an exact generated-only slice of `org.cryptomator.event`. It removes the two-class `FileSystemEventAggregator_Factory` and two-class `NotificationManager_Factory` families. The retained native Dagger component's switching providers directly invoke both functional constructors and have no edge to the standalone factories; no reflection or service metadata names them. The fail-closed package union retains exactly nine functional answer, aggregation, bucket, notification, handler, and vault-event classes.

The thirty-second physical filter is an exact generated-only slice of `org.cryptomator.logging`. It removes only `DebugMode_Factory`. The native Dagger component has no edge to either the legacy GUI's `DebugMode` or its standalone factory; no reflection, configuration, or service metadata names the factory. The fail-closed package union retains `DebugMode`, both rolling/launch policies, `LogbackConfigurator`, and both configurator-factory classes. Native logging initialization and its dedicated XML configuration remain unchanged.

The thirty-third physical filter is an exact generated-only slice of the direct `org.cryptomator.common` package. It removes `LicenseChecker_Factory`, `LicenseHolder_Factory`, and the two-class `CommonsModule_ProvideLicensePublicKeyFactory` family. These wrappers belong to the excluded legacy GUI graph; the retained native Dagger component has no edge to them, and no reflection, configuration, or service metadata names them. The fail-closed direct-package union retains exactly thirty-four other classes, including functional `LicenseChecker`, `LicenseHolder`, `CommonsModule`, and all shared executor, environment, settings, shutdown, reveal-path, security, and utility classes.

The thirty-fourth physical filter extends the exact generated-only direct `org.cryptomator.common` slice. It additionally removes `CommonsModule_ProvideExecutorServiceFactory` and the two-class `CommonsModule_ProvideRevealPathServiceFactory` family. These wrappers belong to the excluded legacy GUI graph; the retained native Dagger component has no edge to them, and no reflection, configuration, or service metadata names them. Functional executor construction and `RevealPathService` provider discovery remain in the retained `CommonsModule`, alongside all Windows integrations and thirty other direct-common classes. The updated fail-closed union now removes seven reviewed wrappers and retains exactly thirty-one classes.

The thirty-fifth physical filter is an exact generated-only slice of `org.cryptomator.common.settings`. It removes `DeviceKey_Factory`, `LegacySettingsAdapter_Factory`, and `SettingsProvider_Factory`. Retained native Dagger bytecode directly constructs `LegacySettingsAdapter` and `SettingsProvider` and has no edge to their standalone factories; `DeviceKey_Factory` is likewise absent from the native graph, reflection, configuration, and service metadata. The fail-closed package union retains exactly sixteen functional device-key, engine/legacy settings, persisted JSON, per-vault settings, theme, and unlock-policy classes.

The thirty-sixth physical filter is an exact generated-only slice of `org.cryptomator.common.mount`. It removes `Mounter_Factory`, `MountModule_ProvideDefaultMountServiceFactory`, `MountServiceSelector_Factory`, and the two-class `WindowsDriveLetters_Factory` family. Retained native Dagger bytecode directly constructs `Mounter`, `MountServiceSelector`, and `WindowsDriveLetters`; the excluded default-provider wrapper is referenced only by the already excluded legacy GUI component. The two generated factory families that provide the supported and used mount-service collections remain because the native component invokes them directly. The fail-closed package union retains exactly twenty-one functional exceptions, mount logic, provider-selection utilities, Windows drive-letter behavior, and required collection factories.

The thirty-seventh physical filter is an exact generated-only slice of `org.cryptomator.common.vaults`. It removes nine standalone factories for auto-locking, legacy state observability, the legacy vault-list graph, snapshot mapping, and vault state. No staged class has an inbound edge to these wrappers. The native per-vault component still references six other generated factories, so those remain with every functional registry, persistence, state, statistics, snapshot, lifecycle, exception, and auto-lock class. The fail-closed package union retains exactly thirty-eight classes, including the functional legacy list module and change listener even though static analysis lists them as candidates.

The thirty-eighth physical filter extends the exact `org.cryptomator.ui.common` slice with six generated-only wrappers: the two-class `DefaultSceneFactory_Factory` family, `StageFactory_Factory`, the two-class `StageInitializer_Factory` family, and `VaultService_Factory`. No staged class has an inbound edge to them. The updated fail-closed union now removes ten reviewed presentation/generated classes and retains exactly thirty functional animation, controller/FXML, stage, task, URL, vault-service, weak-binding, and Windows-caption compatibility classes.

The thirty-ninth physical filter extends the exact `org.cryptomator.ui.fxapp` slice with six generated-only wrappers: `AppLaunchEventHandler_Factory`, `AutoUnlocker_Factory`, `FxApplicationTerminator_Factory`, `FxApplicationWindows_Factory`, `FxFSEventList_Factory`, and `FxNotificationManager_Factory`. No staged class has an inbound edge to them. The updated fail-closed union now removes nineteen reviewed root/generated classes and retains exactly fifteen operational launch handling, auto-unlock, termination, window, filesystem-event, notification, reveal-path, appearance, scope, and primary-stage compatibility classes.

The fortieth physical filter extends the exact `org.cryptomator.ui.mainwindow` slice with the two-class `MainWindowNavigation_Factory` family and `MainWindowSceneFactory_Factory`. No staged class has an inbound edge to these generated wrappers. Functional `MainWindowNavigation`, its destination enum, and `MainWindowSceneFactory` remain. The updated fail-closed union now removes eleven reviewed presentation/generated classes and retains exactly forty-seven diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes.

The forty-first physical filter extends the exact `org.cryptomator.ui.mainwindow` slice with `HowItWorksController_Factory` and `MainWindowController_Factory`. No staged class has an inbound edge to either generated wrapper. Both functional controllers remain. The updated fail-closed union now removes thirteen reviewed presentation/generated classes and retains exactly forty-five diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes.

The forty-second physical filter extends the exact `org.cryptomator.ui.mainwindow` slice with the six generated vault-detail controller wrappers: `VaultDetailController_Factory`, `VaultDetailLockedController_Factory`, `VaultDetailMissingVaultController_Factory`, `VaultDetailNeedsMigrationController_Factory`, `VaultDetailUnknownErrorController_Factory`, and `VaultDetailUnlockedController_Factory`. Each wrapper is self-only in the staged bytecode and has no inbound edge from another class. All six functional vault-detail controllers remain. The updated fail-closed union now removes nineteen reviewed presentation/generated classes and retains exactly thirty-nine diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes.

The forty-third physical filter extends the exact `org.cryptomator.ui.mainwindow` slice with the three remaining generated controller wrappers: `VaultListContextMenuController_Factory`, `VaultListController_Factory`, and `WelcomeController_Factory`. Each wrapper is self-only in the staged bytecode and has no inbound edge from another class. All three functional controllers remain. The updated fail-closed union now removes twenty-two reviewed presentation/generated classes and retains exactly thirty-six diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes.

The forty-fourth physical filter extends the exact `org.cryptomator.ui.mainwindow` slice with nine generated `MainWindowModule` provider-wrapper classes: the embedded-event loader, embedded Preferences window, error stage, FXML loader, main scene, main window, selected Preferences tab, and two-class selected-vault factory families. Seven wrappers are self-only in the staged bytecode; the selected-vault wrapper and its holder reference only their own two-class family. Functional `MainWindowModule` and every other retained main-window class remain. The updated fail-closed union now removes thirty-one reviewed presentation/generated classes and retains exactly twenty-seven functional diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes.

The mandatory ten-interaction local checkpoint passed after slice forty-four. The exact x64 Release output targeted by the development shortcut was fully rebuilt, its EXE and DLL both verified with a valid `CN=VaultKind Development` signature, and VaultKind closed and reopened normally through the shortcut. The responsive live process reported `VaultKind Java Engine`, the permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, and the intact locked `VK1` registration at `F:\VK1\VK1`; Greg confirmed the reopened app functions normally. All 67 native checks passed against the rebuilt state.

The forty-fifth physical filter extends the exact `org.cryptomator.ui.health` slice with four generated presentation-controller wrappers: `CheckDetailController_Factory`, `CheckListController_Factory`, `ResultListCellController_Factory`, and `StartController_Factory`. Each fully qualified wrapper is self-only in the staged bytecode and has no inbound edge from another class. All functional controllers, check execution, report writing, result fixes, encrypted-vault health/repair behavior, and module/provider wiring remain. The updated fail-closed union now removes fifteen reviewed presentation/generated classes and retains exactly forty-two functional health/repair classes.

The forty-sixth physical filter extends the exact `org.cryptomator.ui.health` slice with three generated workflow-helper wrappers: `CheckExecutor_Factory`, `ReportWriter_Factory`, and `ResultFixApplier_Factory`. No staged class has an inbound edge to any wrapper. The functional implementations and their nested task/error classes remain with every controller, encrypted-vault health/repair behavior, and module/provider class. The updated fail-closed union now removes eighteen reviewed presentation/generated classes and retains exactly thirty-nine functional health/repair classes.

The forty-seventh physical filter extends the exact `org.cryptomator.ui.health` slice with six generated presentation/window provider wrappers in `HealthCheckModule`: the FXML-loader factory, check-list scene, start scene, stage, unlock window, and window-showing listener wrappers. No staged class has an inbound edge to any wrapper. `HealthCheckModule`, all available-check, key-loading, selected-check, masterkey, and vault-config providers, every functional controller/helper, and all encrypted-vault health/repair behavior remain. The updated fail-closed union now removes twenty-four reviewed presentation/generated classes and retains exactly thirty-three functional health/repair classes.

The forty-eighth physical filter adds an exact fail-closed `org.cryptomator.ui.keyloading.masterkeyfile` slice and removes only `ChooseMasterkeyFileController_Factory` and `PassphraseEntryController_Factory`. No staged class has an inbound edge to either generated wrapper. Both functional controllers and all twenty-one component, module, result, stored-password, key-loading strategy, password-entry, and masterkey-handling classes remain.

The forty-ninth physical filter extends the exact `org.cryptomator.ui.keyloading.masterkeyfile` slice with `ChooseMasterkeyFileModule_ProvideChooseMasterkeySceneFactory` and `PassphraseEntryModule_ProvideUnlockSceneFactory`. No staged class has an inbound edge to either generated presentation-scene wrapper. Both modules, controllers, components, result providers, stored-password and key-loading strategy, password-entry behavior, and all masterkey handling remain among exactly nineteen retained functional classes.

The fiftieth physical filter adds an exact fail-closed direct `org.cryptomator.ui.keyloading` slice and removes only `KeyLoadingModule_ProvideFxmlLoaderFactoryFactory`. No staged class has an inbound edge to this generated presentation-only wrapper. `KeyLoading`, both component classes, `KeyLoadingModule`, its strategy provider, the scoped annotation, and all three strategy classes remain among exactly eight retained functional classes.

The fifty-first physical filter extends the exact `org.cryptomator.ui.keyloading.masterkeyfile` slice with the two-class `ChooseMasterkeyFileModule_ProvideResultFactory` and `PassphraseEntryModule_ProvideResultFactory` families. Each wrapper/holder pair references only its own two-class family, with no outside staged inbound edge. Both functional result objects, modules, controllers, components, stored-password and loading-strategy providers, password-entry behavior, and all masterkey handling remain among exactly fifteen retained functional classes.

The retention-boundary audit of the fifteen remaining masterkey-file classes is complete. `MasterkeyFileLoadingModule_ProvideStoredPasswordFactory` loads saved credentials through `KeychainManager`; `MasterkeyFileLoadingStrategy_Factory` constructs the strategy that performs password entry, masterkey-file selection, backup, keychain save/cleanup, cancellation, and unlock recovery. The component builders, functional controllers, modules, result object, and scope annotations form that same Dagger workflow. All fifteen remain protected despite static candidacy.

The first runtime-artifact filter removes exactly `runtime/lib/jawt.lib` and `runtime/lib/jvm.lib`. These Windows import libraries support compiling/linking native programs against AWT or the JVM; VaultKind instead launches the bundled `runtime/bin/java.exe` as a separate process. The filter fails closed if a future JDK changes the direct runtime `.lib` set. Removing both saves 1,226,370 bytes, and the trimmed runtime passes the complete bundled-engine lifecycle probe.

The second runtime-artifact filter removes exactly five standalone Java tools that VaultKind does not invoke: `jabswitch.exe`, `jaccessinspector.exe`, `jaccesswalker.exe`, `jfr.exe`, and `keytool.exe`. The pre-removal audit corrected the preliminary boundary after proving that the native host launches `javaw.exe`; both `javaw.exe` and the probe's `java.exe` therefore remain. The filter fails closed against the complete seven-file jlink executable inventory. A direct hidden `javaw.exe -version` smoke exits successfully, and `java.exe` passes the complete bundled-engine lifecycle probe.

The mandatory ten-interaction local checkpoint passed after the fifty-first class slice and second runtime-artifact slice. The exact x64 Release output targeted by the desktop shortcut was fully rebuilt, and its EXE and DLL both report `Valid`, `Signature verified`, with signer `CN=VaultKind Development`. VaultKind closed normally and reopened through `VaultKind Development.lnk`; the shortcut resolved to the rebuilt Release executable and the process remained responsive. Live readback reported `VaultKind Java Engine`, the permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, and exactly one locked vault, `VK1` at `F:\VK1\VK1`. Greg confirmed the reopened app runs and functions correctly. All 67 native checks, the class/provider audit, direct hidden `javaw.exe` smoke, and complete isolated disposable-vault probe passed against the rebuilt state.

The third runtime-artifact filter removes exactly `runtime/lib/psfont.properties.ja`, whose own header identifies it as the Japanese PostScript printer property file. Native startup fixes the Java locale to English, and the only authored Java printing caller is the inherited JavaFX recovery-key display controller already omitted from the native package. The exact guard fails closed if the JDK's `psfont.properties.*` set changes. General Java 2D/PostScript mapping, font configuration, DLLs, configuration, security policy and certificates, time-zone and character-set data, and all legal notices remain intact.

The fourth runtime-artifact filter removes exactly `runtime/lib/jrt-fs.jar`. OpenJDK documents this duplicate provider jar as support for tools running on JDK 8 that need to inspect the shipped modular JDK image. VaultKind ships no JDK 8 tooling, does not place the jar on the engine classpath, and has no staged `jrt:` consumer. The Java 26 `java.base` module retains the same `JrtFileSystemProvider`, and the trimmed runtime successfully enumerates its complete module image. The exact guard fails closed if the JDK's direct `runtime/lib/*.jar` set changes.

The fifth runtime-artifact filter removes exactly `runtime/lib/jfr/default.jfc` and `runtime/lib/jfr/profile.jfc`. Oracle documents them as optional named templates loaded when Flight Recorder is explicitly started. VaultKind's fixed engine command has no recording flag, no staged class/library references the JFR API or templates, and the package contains neither `jfr.exe` nor `jcmd`. The `jdk.jfr`, `jdk.management`, and `jdk.management.jfr` modules remain intact. The exact guard fails closed if the JDK's profile-file set changes.

The sixth runtime-artifact filter removes exactly `runtime/conf/sound.properties`. The file contains only optional Java MIDI and sampled-audio provider overrides. Source and compiled-dependency scans find no retained Java Sound caller, and all 55 staged libraries contain zero Java Sound service-provider descriptors. VaultKind's three signature sounds are independent native WinUI/.NET `MediaPlayer` paths: the release carries its open and locked MP3 assets separately and uses Windows `Alarm06.wav` or a native generated-WAV fallback for warnings. The complete `java.desktop` module and native audio support remain intact, and the exact `*sound*` configuration guard fails closed if the JDK changes this reviewed set.

The seventh runtime-artifact filter removes exactly `runtime/conf/jaxp-strict.properties.template`. Oracle's Java 26 documentation defines it as an opt-in compatibility-testing template that must first be copied to a `.properties` file and then selected with `-Djava.xml.config.file`. Neither fixed engine launcher sets that property, and no source, staged class, or staged library references the template or property. The active `jaxp.properties`, its XML security limits, the complete `java.xml` module, and all parser implementations remain intact. The exact `*.template` guard fails closed if the JDK changes this reviewed set.

The eighth runtime-artifact filter removes exactly `runtime/lib/classlist`. Its header and Oracle's Java 26 documentation identify it only as input to an explicit `-Xshare:dump` archive-creation operation. This jlink image contains no `.jsa` archive, both fixed launch paths omit all CDS creation options, and normal startup diagnostics already report that no shared archive is mapped. Removing the orphaned input therefore cannot reduce current startup performance. The build fails closed if a future runtime contains any `.jsa` archive so that its performance benefit is reviewed before this filter can run.

The complete stage hash audit found five duplicate groups totaling only 9,430 theoretical bytes. Four groups totaling 2,792 bytes are required per-module Java legal stubs and limited/unlimited security-policy copies and remain protected. The sole accidental duplicate was the 6,638-byte generated `THIRD-PARTY.txt`: the retired JavaFX About controller is absent from the native stage, while the release already publishes the identical source in `Notices`. Staging now requires and publishes that notice exactly once, and its hash is checked against the generated source during validation.

The remaining runtime configuration, module, security, font, timezone, character-set, metadata, and legal files are protected. The next safe packaging step is a read-only dependency and import audit of the root `onnxruntime.dll` and `DirectML.dll` pair, which together account for about 40 MB. Determine why Windows App SDK publishing includes them, prove whether any retained managed or native binary imports or dynamically loads them, and consult supported deployment semantics before considering an exact exclusion. Do not remove either file, AI/ML-related dependencies, or other root runtime binaries during the audit.

## Required validation after this boundary

1. Complete Java test suite.
2. Native policy/protocol test suite.
3. Fresh x64 staged release build.
4. Assertion that required resources exist and inherited presentation directories do not.
5. Bundled-engine isolated disposable-vault protocol probe, including malformed-client recovery, password/recovery rotation, fixture cleanup, and graceful shutdown.

Mounted-vault and provider testing remains a separate milestone and must use intentionally disposable vaults.

## Validation result

The boundary passed on July 28, 2026:

- 315 Java tests passed with zero failures and zero errors; two platform-specific tests were skipped.
- 66 native policy, protocol, persistence, keyboard, documentation, and workflow checks passed.
- The fresh x64 stage contains 1,101 files under `Engine/classes`, including all compiled classes and required resources.
- `fxml`, `css`, and `img` are absent from staged engine classes; English messages, the recovery word list, and native logging configuration are present.
- The bundled engine passed `backend.hello` and graceful shutdown.
- The stage is 279,876,142 bytes (266.91 MiB). The removed presentation resources total 3,042,203 bytes (2.90 MiB), compared with the same stage contents before this filter.

The later permanent English-only cleanup removed two additional compiled language-policy classes plus the language/orientation machinery. The refreshed stage contains 1,099 files under `Engine/classes` and is 279,861,126 bytes (266.90 MiB). Only the `en-us` WinUI satellite directory, the English string catalog, and the English recovery-word list remain as authored language assets.

The dedicated `NativeBackendMain` validation also passed all 315 Java tests (two platform skips), all 66 native checks, a fresh x64 stage, and the bundled-engine smoke test. Its extra entry class brings the current stage to 279,862,701 bytes and 1,100 files under `Engine/classes`; this is a 1,575-byte increase over the prior English-only stage and establishes a clean root for the class inventory above.

The neutral masterkey-scheme boundary passed all 315 Java tests (two platform skips), a fresh x64 stage, and the bundled-engine smoke test. The refreshed stage is 279,862,559 bytes; the 142-byte physical reduction is incidental because compiled candidates remain packaged until runtime retention evidence is complete.

The expanded zero-vault probe passed on July 28, 2026. It exercises malformed-client isolation, backend identity, empty vault listing, current mount-provider discovery, all 11 missing-vault command paths, unsupported protocol, unknown operation, and graceful shutdown against the staged runtime. The bridge now contains malformed input to the offending connection so a fresh client can reconnect. All 315 Java tests pass with zero failures/errors (two platform skips), all 66 native checks pass, and the refreshed unfiltered x64 stage is 279,862,721 bytes. No real vault, password, recovery key, mount, provider selection, or user profile was touched.

The first physical class slice passed on July 29, 2026. The filtered stage contains 1,089 authored classes: the same 145 remain statically reachable and 944 remain candidates after the six reviewed dialog classes were omitted. `Engine/classes` contains 1,094 class/resource files, no dialog class survived, and the complete stage is 279,838,327 bytes. This is an exact 24,394-byte reduction from the immediately preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The second physical class slice passed on July 29, 2026. The filtered stage contains 1,079 authored classes: the same 145 remain statically reachable and 934 remain candidates after the ten reviewed wrong-file-alert classes were omitted. `Engine/classes` contains 1,084 class/resource files, neither reviewed package survives, and the complete stage is 279,817,672 bytes. The second slice saves exactly 20,655 bytes; both slices save 45,049 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The third physical class slice passed on July 29, 2026. The filtered stage contains 1,069 authored classes: the same 145 remain statically reachable and 924 remain candidates after the ten reviewed update-reminder classes were omitted. `Engine/classes` contains 1,074 class/resource files, none of the three reviewed packages survives, and the complete stage is 279,797,490 bytes. The third slice saves exactly 20,182 bytes; all three slices save 65,231 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The fourth physical class slice passed on July 29, 2026. The filtered stage contains 1,059 authored classes: the same 145 remain statically reachable and 914 remain candidates after the ten reviewed share-vault classes were omitted. `Engine/classes` contains 1,064 class/resource files, none of the four reviewed packages survives, and the complete stage is 279,774,392 bytes. The fourth slice saves exactly 23,098 bytes; all four slices save 88,329 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The fifth physical class slice passed on July 29, 2026. The filtered stage contains 1,047 authored classes: the same 145 remain statically reachable and 902 remain candidates after the twelve reviewed statistics-window classes were omitted. `Engine/classes` contains 1,052 class/resource files, none of the five reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,738,622 bytes. The fifth slice saves exactly 35,770 bytes; all five slices save 124,099 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The sixth physical class slice passed on July 29, 2026. The filtered stage contains 1,036 authored classes: the same 145 remain statically reachable and 891 remain candidates after the eleven reviewed filename-inspection classes were omitted. `Engine/classes` contains 1,041 class/resource files, none of the six reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,697,455 bytes. The sixth slice saves exactly 41,167 bytes; all six slices save 165,266 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The seventh physical class slice passed on July 29, 2026. The filtered stage contains 1,025 authored classes: the same 145 remain statically reachable and 880 remain candidates after the eleven reviewed JavaFX error-window classes were omitted. `Engine/classes` contains 1,030 class/resource files, none of the seven reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,664,538 bytes. The seventh slice saves exactly 32,917 bytes; all seven slices save 198,183 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The eighth physical class slice passed on July 29, 2026. The filtered stage contains 1,010 authored classes: the same 145 remain statically reachable and 865 remain candidates after the fifteen reviewed JavaFX control classes were omitted. `Engine/classes` contains 1,015 class/resource files, none of the eight reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,602,233 bytes. The eighth slice saves exactly 62,305 bytes; all eight slices save 260,488 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged.

The ninth physical class slice passed on July 29, 2026. The filtered stage contains 995 authored classes: the same 145 remain statically reachable and 850 remain candidates after the fifteen reviewed JavaFX quit-dialog classes were omitted. `Engine/classes` contains 1,000 class/resource files, none of the nine reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,558,478 bytes. The ninth slice saves exactly 43,755 bytes; all nine slices save 304,243 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe, including graceful shutdown, passed unchanged.

The tenth physical class slice passed on July 29, 2026. The filtered stage contains 978 authored classes: the same 145 remain statically reachable and 833 remain candidates after the seventeen reviewed JavaFX lock-workflow classes were omitted. `Engine/classes` contains 983 class/resource files, none of the ten reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,515,959 bytes. The tenth slice saves exactly 42,519 bytes; all ten slices save 346,762 bytes cumulatively from the preceding unfiltered stage. The complete zero-vault bundled-engine probe passed unchanged, including mount-provider inventory and graceful shutdown.

The eleventh physical class slice passed on July 29, 2026. The filtered stage contains 964 authored classes: the same 145 remain statically reachable and 819 remain candidates after the fourteen reviewed JavaFX password-window classes were omitted. `Engine/classes` contains 969 class/resource files, none of the eleven reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,468,742 bytes. The eleventh slice saves exactly 47,217 bytes; all eleven slices save 393,979 bytes cumulatively from the preceding unfiltered stage. The complete isolated disposable-vault probe passed unchanged, including password change, retired-password rejection, recovery reset, mount-provider inventory, and graceful shutdown.

The twelfth physical class slice passed on July 29, 2026. The filtered stage contains 916 authored classes: the same 145 remain statically reachable and 771 remain candidates after the forty-eight reviewed JavaFX recovery-wizard classes were omitted. `Engine/classes` contains 921 class/resource files, none of the twelve reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,278,325 bytes. The twelfth slice saves exactly 190,417 bytes; all twelve slices save 584,396 bytes cumulatively from the preceding unfiltered stage. The complete isolated disposable-vault probe passed unchanged, including recovery-key creation and display, wrong-password rejection, password reset, retired-password rejection, key stability, mount-provider inventory, and graceful shutdown.

The thirteenth physical class slice passed on July 29, 2026. The filtered stage contains 869 authored classes: the same 145 remain statically reachable and 724 remain candidates after the forty-seven reviewed JavaFX add-vault wizard classes were omitted. `Engine/classes` contains 874 class/resource files, none of the thirteen reviewed packages survives, and all ten tray-menu classes remain. The complete stage is 279,091,112 bytes. The thirteenth slice saves exactly 187,213 bytes; all thirteen slices save 771,609 bytes cumulatively from the preceding unfiltered stage. The complete isolated disposable-vault probe passed unchanged, including vault creation, registration, recovery-key generation and display, removal, existing-vault reconnection, mount-provider inventory, and graceful shutdown.

The fourteenth physical class slice passed on July 29, 2026. The filtered stage contains 857 authored classes: the same 145 remain statically reachable and 712 remain candidates after the twelve reviewed JavaFX forget-password classes were omitted. `Engine/classes` contains 862 class/resource files, none of the fourteen reviewed packages survives, all seven common keychain classes remain, and all ten tray-menu classes remain. The complete stage is 279,064,266 bytes. The fourteenth slice saves exactly 26,846 bytes; all fourteen slices save 798,455 bytes cumulatively from the preceding unfiltered stage. The complete isolated disposable-vault probe passed unchanged, including password and recovery rotation, mount-provider inventory, and graceful shutdown.

The fifteenth physical class slice passed on July 29, 2026. The filtered stage contains 845 authored classes: the same 145 remain statically reachable and 700 remain candidates after the twelve reviewed legacy-updater classes were omitted. `Engine/classes` contains 850 class/resource files, none of the fifteen reviewed packages survives, all seven common keychain classes remain, and all ten tray-menu classes remain. The complete stage is 279,023,649 bytes. The fifteenth slice saves exactly 40,617 bytes; all fifteen slices save 839,072 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The sixteenth physical class slice passed on July 29, 2026. The filtered stage contains 824 authored classes: the same 145 remain statically reachable and 679 remain candidates after the twenty-one reviewed Hub-to-password JavaFX conversion classes were omitted. `Engine/classes` contains 829 class/resource files, none of the sixteen reviewed packages survives, all seventy Hub key-loading classes remain, and all ten tray-menu classes remain. The complete stage is 278,957,056 bytes. The sixteenth slice saves exactly 66,593 bytes; all sixteen slices save 905,665 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The seventeenth physical class slice passed on July 29, 2026. The filtered stage contains 797 authored classes: the same 145 remain statically reachable and 652 remain candidates after the twenty-seven reviewed JavaFX Vault Options classes were omitted. `Engine/classes` contains 802 class/resource files, none of the seventeen reviewed packages survives, all twenty-five vault-format migration classes remain, and all ten tray-menu classes remain. The complete stage is 278,853,804 bytes. The seventeenth slice saves exactly 103,252 bytes; all seventeen slices save 1,008,917 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The eighteenth physical class slice passed on July 29, 2026. The filtered stage contains 768 authored classes: the same 145 remain statically reachable and 623 remain candidates after the twenty-nine reviewed JavaFX Preferences classes were omitted. `Engine/classes` contains 773 class/resource files, none of the eighteen reviewed packages survives, all twenty-five vault-format migration classes remain, and all ten tray-menu classes remain. The complete stage is 278,741,052 bytes. The eighteenth slice saves exactly 112,752 bytes; all eighteen slices save 1,121,669 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The nineteenth physical class slice passed on July 29, 2026. The filtered stage contains 744 authored classes: the same 145 remain statically reachable and 599 remain candidates after the twenty-four reviewed JavaFX unlock-window/workflow classes were omitted. `Engine/classes` contains 749 class/resource files; `UnlockCancelledException` is the sole retained class in `org.cryptomator.ui.unlock`, and all twenty-five vault-format migration and ten tray-menu classes remain. The complete stage is 278,658,461 bytes. The nineteenth slice saves exactly 82,591 bytes; all nineteen slices save 1,204,260 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including password unlock, wrong-password rejection, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twentieth physical class slice passed on July 29, 2026. The filtered stage contains 733 authored classes: the same 145 remain statically reachable and 588 remain candidates after the eleven reviewed JavaFX notification-window classes were omitted. `Engine/classes` contains 738 class/resource files; reachable `NotificationManager` and `VaultEvent`, the `NotificationHandler` service interface, `UnlockCancelledException`, all twenty-five vault-format migration classes, and all ten tray-menu classes remain. The complete stage is 278,624,802 bytes. The twentieth slice saves exactly 33,659 bytes; all twenty slices save 1,237,919 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including password/recovery operations, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-first physical class slice passed on July 29, 2026. The filtered stage contains 714 authored classes: the same 145 remain statically reachable and 569 remain candidates after the nineteen reviewed JavaFX event-viewer classes were omitted. `Engine/classes` contains 719 class/resource files; reachable `FileSystemEventAggregator`, `FSEventBucket`, `NotificationManager`, `VaultEvent`, the `NotificationHandler` service interface, `UnlockCancelledException`, all twenty-five vault-format migration classes, and all ten tray-menu classes remain. The complete stage is 278,556,886 bytes. The twenty-first slice saves exactly 67,916 bytes; all twenty-one slices save 1,305,835 bytes cumulatively from the preceding unfiltered stage. All 66 native checks and the complete isolated disposable-vault probe passed unchanged, including password/recovery operations, mount-provider inventory, protocol errors, and graceful shutdown. The probe does not synthesize live cryptofs conflict, broken-node, decryption-failure, or cross-device file-in-use events, so native presentation and actions for those events remain a dedicated workflow and coverage gap.

The twenty-second physical class slice passed on July 29, 2026. The filtered stage contains 703 authored classes: the same 145 remain statically reachable and 558 remain candidates after the eleven reviewed JavaFX health rendering helpers were omitted. `Engine/classes` contains 708 class/resource files, while every health execution, cryptographic repair, key-loading, reporting, workflow-controller, and Dagger class listed above remains. The complete stage is 278,529,087 bytes. The twenty-second slice saves exactly 27,799 bytes; all twenty-two slices save 1,333,634 bytes cumulatively from the preceding unfiltered stage. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-third physical class slice passed on July 29, 2026. The filtered stage contains 695 authored classes: the same 145 remain statically reachable and 550 remain candidates after the eight reviewed JavaFX main-window presentation helpers were omitted. `Engine/classes` contains 700 class/resource files; all fifty retained main-window lifecycle, diagnostic, navigation, shortcut, and Dagger classes remain. The complete stage is 278,507,713 bytes. The twenty-third slice saves exactly 21,374 bytes; all twenty-three slices save 1,355,008 bytes cumulatively from the preceding unfiltered stage. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-fourth physical class slice passed on July 29, 2026. The filtered stage contains 682 authored classes: the same 145 remain statically reachable and 537 remain candidates after the thirteen reviewed JavaFX application-root classes were omitted. `Engine/classes` contains 687 class/resource files, and all twenty-one operational `ui.fxapp` classes listed above remain. The complete stage is 278,464,609 bytes. The twenty-fourth slice saves exactly 43,104 bytes; all twenty-four slices save 1,398,112 bytes cumulatively from the preceding unfiltered stage. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-fifth physical class slice passed on July 29, 2026. The filtered stage contains 551 authored classes: the same 145 remain statically reachable and 406 remain candidates after the 131 retired JavaFX/IPC launcher classes were omitted. `Engine/classes` contains 556 class/resource files, and the staged launcher directory contains exactly the twenty retained native/shared classes. The complete stage is 277,797,924 bytes. The twenty-fifth slice saves exactly 666,685 bytes; all twenty-five slices save 2,064,797 bytes cumulatively from the preceding unfiltered stage. All 67 native checks and the complete isolated disposable-vault probe passed unchanged through the retained `NativeBackendMain`, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-sixth physical class slice passed on July 29, 2026. The filtered stage contains 547 authored classes: the same 145 remain statically reachable and 402 remain candidates after the four reviewed `ui.common` presentation helpers were omitted. `Engine/classes` contains 552 class/resource files, and the common package contains exactly the thirty-six retained workflow and compatibility classes. The complete stage is 277,792,136 bytes. The twenty-sixth slice saves exactly 5,788 bytes; all twenty-six slices save 2,070,585 bytes cumulatively from the preceding unfiltered stage. The 19 service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including create/connect, password and recovery rotation, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-seventh physical class slice passed on July 29, 2026. The filtered stage contains 543 authored classes: the same 145 remain statically reachable and 398 remain candidates after the four reviewed macOS/Linux networking classes were omitted. `Engine/classes` contains 548 class/resource files, and the networking package contains exactly the seven retained Windows TLS/provider classes. The complete stage is 277,783,533 bytes. Slice twenty-seven saves exactly 8,603 bytes; all twenty-seven slices save 2,079,188 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-eighth physical class slice passed on July 29, 2026. The filtered stage contains 534 authored classes: the same 145 remain statically reachable and 389 remain candidates after the exact nine legacy IPC classes were omitted. `Engine/classes` contains 539 class/resource files and no IPC class survives. The complete stage is 277,755,401 bytes. Slice twenty-eight saves exactly 28,132 bytes; all twenty-eight slices save 2,107,320 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged through `NativeUiBridge`, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The twenty-ninth physical class slice passed on July 29, 2026. The filtered stage contains 533 authored classes: the same 145 remain statically reachable and 388 remain candidates after the test-only JavaFX startup helper was omitted. `Engine/classes` contains 538 class/resource files and no authored root-package class survives. The complete stage is 277,753,814 bytes. Slice twenty-nine saves exactly 1,587 bytes; all twenty-nine slices save 2,108,907 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirtieth physical class slice passed on July 29, 2026. The filtered stage contains 524 authored classes: the same 145 remain statically reachable and 379 remain candidates after the nine unused generated native factories were omitted. `Engine/classes` contains 529 class/resource files, and `org.cryptomator.nativeui` contains exactly the twenty-one retained functional classes. The complete stage is 277,728,950 bytes. Slice thirty saves exactly 24,864 bytes; all thirty slices save 2,133,771 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-first physical class slice passed on July 29, 2026. The filtered stage contains 520 authored classes: the same 145 remain statically reachable and 375 remain candidates after the four unused generated event factories were omitted. `Engine/classes` contains 525 class/resource files, and `org.cryptomator.event` contains exactly the nine retained functional classes. The complete stage is 277,724,996 bytes. Slice thirty-one saves exactly 3,954 bytes; all thirty-one slices save 2,137,725 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-second physical class slice passed on July 29, 2026. The filtered stage contains 519 authored classes: the same 145 remain statically reachable and 374 remain candidates after the unused generated debug-mode factory was omitted. `Engine/classes` contains 524 class/resource files, and `org.cryptomator.logging` contains exactly the six retained functional logging classes. The complete stage is 277,723,089 bytes. Slice thirty-two saves exactly 1,907 bytes; all thirty-two slices save 2,139,632 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-third physical class slice passed on July 29, 2026. The filtered stage contains 515 authored classes: the same 145 remain statically reachable and 370 remain candidates after the four unused generated license wrappers were omitted. `Engine/classes` contains 520 class/resource files, and the direct `org.cryptomator.common` package contains exactly the thirty-four retained classes, including both functional license classes. The complete stage is 277,716,570 bytes. Slice thirty-three saves exactly 6,519 bytes; all thirty-three slices save 2,146,151 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-fourth physical class slice passed on July 29, 2026. The filtered stage contains 512 authored classes: the same 145 remain statically reachable and 367 remain candidates after the three additional generated direct-common wrappers were omitted. `Engine/classes` contains 517 class/resource files, and the direct `org.cryptomator.common` package contains exactly the thirty-one retained classes. The complete stage is 277,712,088 bytes. Slice thirty-four saves exactly 4,482 bytes; all thirty-four slices save 2,150,633 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-fifth physical class slice passed on July 29, 2026. The filtered stage contains 509 authored classes: the same 145 remain statically reachable and 364 remain candidates after the three unused generated settings factories were omitted. `Engine/classes` contains 514 class/resource files, and `org.cryptomator.common.settings` contains exactly the sixteen retained functional classes. The complete stage is 277,704,403 bytes. Slice thirty-five saves exactly 7,685 bytes; all thirty-five slices save 2,158,318 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-sixth physical class slice passed on July 29, 2026. The filtered stage contains 504 authored classes: the same 145 remain statically reachable and 359 remain candidates after the five unused generated mount wrappers were omitted. `Engine/classes` contains 509 class/resource files, and `org.cryptomator.common.mount` contains exactly the twenty-one retained functional and native-required classes. The complete stage is 277,691,662 bytes. Slice thirty-six saves exactly 12,741 bytes; all thirty-six slices save 2,171,059 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-seventh physical class slice passed on July 29, 2026. The filtered stage contains 495 authored classes: the same 145 remain statically reachable and 350 remain candidates after the nine unused generated vault wrappers were omitted. `Engine/classes` contains 500 class/resource files, and `org.cryptomator.common.vaults` contains exactly the thirty-eight retained functional and native-required classes. The complete stage is 277,665,753 bytes. Slice thirty-seven saves exactly 25,909 bytes; all thirty-seven slices save 2,196,968 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-eighth physical class slice passed on July 29, 2026. The filtered stage contains 489 authored classes: the same 145 remain statically reachable and 344 remain candidates after the six additional generated common-UI wrappers were omitted. `Engine/classes` contains 494 class/resource files, and `org.cryptomator.ui.common` contains exactly the thirty retained functional compatibility/workflow classes. The complete stage is 277,657,128 bytes. Slice thirty-eight saves exactly 8,625 bytes; all thirty-eight slices save 2,205,593 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The thirty-ninth physical class slice passed on July 29, 2026. The filtered stage contains 483 authored classes: the same 145 remain statically reachable and 338 remain candidates after the six additional generated FX-application wrappers were omitted. `Engine/classes` contains 488 class/resource files, and `org.cryptomator.ui.fxapp` contains exactly the fifteen retained operational compatibility classes. The complete stage is 277,623,832 bytes. Slice thirty-nine saves exactly 33,296 bytes; all thirty-nine slices save 2,238,889 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The fortieth physical class slice passed on July 29, 2026. The filtered stage contains 480 authored classes: the same 145 remain statically reachable and 335 remain candidates after the three generated main-window navigation/scene wrappers were omitted. `Engine/classes` contains 485 class/resource files, and `org.cryptomator.ui.mainwindow` contains exactly the forty-seven retained functional workflow classes. The complete stage is 277,618,968 bytes. Slice forty saves exactly 4,864 bytes; all forty slices save 2,243,753 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-first physical class slice passed on July 29, 2026. The filtered stage contains 478 authored classes: the same 145 remain statically reachable and 333 remain candidates after the two generated main-window controller wrappers were omitted. `Engine/classes` contains 483 class/resource files, and `org.cryptomator.ui.mainwindow` contains exactly the forty-five retained functional workflow classes. The complete stage is 277,608,922 bytes. Slice forty-one saves exactly 10,046 bytes; all forty-one slices save 2,253,799 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-second physical class slice passed on July 29, 2026. The filtered stage contains 472 authored classes: the same 145 remain statically reachable and 327 remain candidates after the six generated vault-detail controller wrappers were omitted. `Engine/classes` contains 477 class/resource files, and `org.cryptomator.ui.mainwindow` contains exactly the thirty-nine retained functional workflow classes. The complete stage is 277,576,470 bytes. Slice forty-two saves exactly 32,452 bytes; all forty-two slices save 2,286,251 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-third physical class slice passed on July 29, 2026. The filtered stage contains 469 authored classes: the same 145 remain statically reachable and 324 remain candidates after the three remaining generated main-window controller wrappers were omitted. `Engine/classes` contains 474 class/resource files, and `org.cryptomator.ui.mainwindow` contains exactly the thirty-six retained functional workflow/module classes. The complete stage is 277,552,354 bytes. Slice forty-three saves exactly 24,116 bytes; all forty-three slices save 2,310,367 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-fourth physical class slice passed on July 29, 2026. The filtered stage contains 460 authored classes: the same 145 remain statically reachable and 315 remain candidates after the nine generated main-window module provider wrappers were omitted. `Engine/classes` contains 465 class/resource files, and `org.cryptomator.ui.mainwindow` contains exactly the twenty-seven retained functional workflow/module classes. The complete stage is 277,529,277 bytes. Slice forty-four saves exactly 23,077 bytes; all forty-four slices save 2,333,444 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-fifth physical class slice passed on July 29, 2026. The filtered stage contains 456 authored classes: the same 145 remain statically reachable and 311 remain candidates after the four generated Vault Doctor presentation-controller wrappers were omitted. `Engine/classes` contains 461 class/resource files, and `org.cryptomator.ui.health` contains exactly the forty-two retained functional health/repair classes. The complete stage is 277,508,946 bytes. Slice forty-five saves exactly 20,331 bytes; all forty-five slices save 2,353,775 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-sixth physical class slice passed on July 29, 2026. The filtered stage contains 453 authored classes: the same 145 remain statically reachable and 308 remain candidates after the three generated Vault Doctor workflow-helper wrappers were omitted. `Engine/classes` contains 458 class/resource files, and `org.cryptomator.ui.health` contains exactly the thirty-nine retained functional health/repair classes. The complete stage is 277,495,986 bytes. Slice forty-six saves exactly 12,960 bytes; all forty-six slices save 2,366,735 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-seventh physical class slice passed on July 29, 2026. The filtered stage contains 447 authored classes: the same 145 remain statically reachable and 302 remain candidates after the six generated Vault Doctor presentation/window provider wrappers were omitted. `Engine/classes` contains 452 class/resource files, and `org.cryptomator.ui.health` contains exactly the thirty-three retained functional health/repair classes. The complete stage is 277,476,850 bytes. Slice forty-seven saves exactly 19,136 bytes; all forty-seven slices save 2,385,871 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-eighth physical class slice passed on July 29, 2026. The filtered stage contains 445 authored classes: the same 145 remain statically reachable and 300 remain candidates after the two generated masterkey-file controller wrappers were omitted. `Engine/classes` contains 450 class/resource files, and `org.cryptomator.ui.keyloading.masterkeyfile` contains exactly the twenty-one retained functional classes. The complete stage is 277,464,723 bytes. Slice forty-eight saves exactly 12,127 bytes; all forty-eight slices save 2,397,998 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The forty-ninth physical class slice passed on July 29, 2026. The filtered stage contains 443 authored classes: the same 145 remain statically reachable and 298 remain candidates after the two generated masterkey-file presentation-scene wrappers were omitted. `Engine/classes` contains 448 class/resource files, and `org.cryptomator.ui.keyloading.masterkeyfile` contains exactly the nineteen retained functional classes. The complete stage is 277,457,725 bytes. Slice forty-nine saves exactly 6,998 bytes; all forty-nine slices save 2,404,996 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The fiftieth physical class slice passed on July 29, 2026. The filtered stage contains 442 authored classes: the same 145 remain statically reachable and 297 remain candidates after the direct key-loading FXML-loader wrapper was omitted. `Engine/classes` contains 447 class/resource files, and the direct `org.cryptomator.ui.keyloading` package contains exactly the eight retained functional classes. The complete stage is 277,453,732 bytes. Slice fifty saves exactly 3,993 bytes; all fifty slices save 2,408,989 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The fifty-first physical class slice passed on July 29, 2026. The filtered stage contains 438 authored classes: the same 145 remain statically reachable and 293 remain candidates after the two generated masterkey-file result-holder families were omitted. `Engine/classes` contains 443 class/resource files, and `org.cryptomator.ui.keyloading.masterkeyfile` contains exactly the fifteen retained functional classes. The complete stage is 277,448,408 bytes. Slice fifty-one saves exactly 5,324 bytes; all fifty-one slices save 2,414,313 bytes cumulatively. The 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed unchanged, including malformed-client isolation, create/connect, password and recovery rotation, rename/remove, all 11 missing-vault commands, mount-provider inventory, protocol errors, and graceful shutdown.

The first runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes the two jlink-produced Windows import libraries, `jawt.lib` and `jvm.lib`, while retaining the complete executable runtime. The complete stage is 276,222,038 bytes. This slice saves exactly 1,226,370 bytes; all class and runtime slices save 3,640,683 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks and the complete isolated disposable-vault probe passed through the trimmed `runtime/bin/java.exe`.

The second runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes five unused administration, diagnostics, and accessibility tool launchers while retaining exactly `java.exe` and `javaw.exe`. The complete stage is 275,949,838 bytes. This slice saves exactly 272,200 bytes; all class and runtime slices save 3,912,883 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The third runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes only the 3,793-byte Japanese PostScript printer mapping while retaining the general PostScript mapping, font configuration, every runtime DLL, all security/configuration/time-zone data, and all legal notices. The complete stage is 275,946,045 bytes; all class and runtime slices save 3,916,676 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The fourth runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes only the 108,925-byte JDK-8 external-tooling copy of the `jrt` filesystem provider. The built-in Java 26 provider remains in `java.base`, the trimmed runtime enumerates all twenty-three modules successfully, and no staged class or library references `jrt:` or `jrt-fs`. The complete stage is 275,837,120 bytes; all class and runtime slices save 4,025,601 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The fifth runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes the two standard Flight Recorder templates totaling 81,544 bytes while retaining all three JFR/management modules. The fixed launch path contains no recording option and the staged classes/libraries contain no JFR/template reference. The complete stage is 275,755,576 bytes; all class and runtime slices save 4,107,145 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The sixth runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes only the 1,210-byte Java Sound provider-override configuration while retaining `java.desktop`, native audio support, and VaultKind's independently packaged and played signature sounds. Retained classes and all 55 staged libraries contain no Java Sound dependency or service-provider descriptor. The complete stage is 275,754,366 bytes; all class and runtime slices save 4,108,355 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, the 19 release-JAR service descriptors and 30 providers remain unchanged, and all twenty-three runtime modules enumerate successfully. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The seventh runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes only the 4,937-byte opt-in strict-JAXP compatibility-testing template. The active `jaxp.properties` and its reviewed XML security limits remain, `java.xml` remains among all twenty-three enumerated modules, and neither fixed launcher nor any source/staged class/library selects a custom JAXP configuration. The complete stage is 275,749,429 bytes; all class and runtime slices save 4,113,292 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All 67 native checks pass, `javaw.exe` exits successfully in a direct hidden runtime smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The eighth runtime-artifact slice passed on July 30, 2026. The exact fail-closed filter removes only the 87,297-byte CDS dump-time class-list input from a runtime that contains no CDS archive. Oracle's documented runtime path consumes a `.jsa` archive, while the class list is supplied only when explicitly creating one; VaultKind supplies neither creation nor archive options. Post-removal CDS diagnostics match the previous successful archive-free startup. The complete stage is 275,662,132 bytes; all class and runtime slices save 4,200,589 bytes cumulatively. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19 release-JAR service descriptors and 30 providers remain unchanged. All twenty-three modules enumerate, all 67 native checks pass, `javaw.exe` exits successfully in a direct hidden smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The complete-stage duplicate cleanup passed on July 30, 2026. Hashing all 869 files found only one accidental duplicate: `THIRD-PARTY.txt` was both an unused native-engine resource and the published notice. The staged engine's legacy JavaFX resource consumer is absent; the build now requires the generated source and publishes it only in `Notices`. The remaining four duplicate groups total 2,792 bytes and are protected Java legal/security-policy files. The complete stage contains 868 files totaling 275,655,494 bytes; cumulative class, runtime, and duplicate cleanup savings are 4,207,227 bytes. The authored/reachable inventory remains 438/145 with 293 static candidates, and the 19/30 provider inventory is unchanged. All twenty-three modules enumerate, all 67 native checks pass, `javaw.exe` exits successfully in a direct hidden smoke, and `java.exe` passes the complete isolated disposable-vault probe.

The Windows App SDK component audit passed on July 30, 2026. Version 2.3.1 of Microsoft's umbrella `Microsoft.WindowsAppSDK` package declares Widgets, AI, and ML component dependencies even though VaultKind has no source reference to those APIs. In a self-contained publish, the ML dependency alone contributed `DirectML.dll`, `onnxruntime.dll`, the Windows ML native and managed projections, and associated payloads. VaultKind now references Microsoft's Base 2.0.4, Foundation 2.3.5, InteractiveExperiences 2.1.3, WinUI 2.3.0, DWrite 2.1.0, and Runtime 2.3.1 component packages directly. The restored graph contains no umbrella, Widgets, AI, ML, or Windows AI Machine Learning package. The build checks that graph and the five reviewed ML output filenames fail closed. The complete stage contains 807 files totaling 224,816,675 bytes, saving 50,838,819 bytes at this boundary and 55,046,046 bytes cumulatively. All 67 native checks, the unchanged 438/145/293 class and 19/30 provider inventories, all twenty-three Java modules, direct hidden `javaw.exe` smoke, and the complete isolated disposable-vault probe pass.

The 26,341,408-byte root `Microsoft.Windows.SDK.NET.dll` was audited and retained. VaultKind directly consumes Windows Runtime activation, foundation, graphics, storage/picker, clipboard, launcher, and media APIs, and the compiled app records an assembly dependency on the Windows SDK projection. Microsoft's C#/WinRT embedded mode can replace the monolithic projection only when the participating WinRT types are self-contained within the app or library binary. VaultKind's official `Microsoft.WinUI.dll` projection also directly references `Microsoft.Windows.SDK.NET`, matching Microsoft's documented WinUI-to-Windows-SDK interop dependency. Embedding only VaultKind's own Windows API subset therefore cannot remove the shared projection, while replacing WinUI's official projection would leave the supported package boundary. No file is removed and stage size is unchanged by this audit.

The compiled PRI audit used Microsoft's MakePri dump facility rather than editing vendor resource indexes. `VaultKind.Windows.pri` contains the Microsoft WinUI control resource map: 218 US-English control strings plus qualified variants across eighty-six languages. Those framework strings support built-in control labels and accessibility behavior and are not VaultKind translation code. The separately shipped `Microsoft.UI.Xaml.Controls.pri` is also a Microsoft framework resource index. Both remain intact. The audit did find one ignored `native/VaultKind.Windows/artifacts/component-test` publish experiment being admitted by WinUI's default image glob as a bogus `artifacts/component-test/Microsoft.UI.Xaml/Assets/NoiseAsset_256x256_PNG.png` app resource. `DefaultItemExcludes` now excludes the project-local `artifacts` tree at MSBuild evaluation. A clean build reduced `VaultKind.Windows.pri` by exactly 256 bytes, from 2,324,784 to 2,324,528 bytes, and its detailed dump now contains 239 named resources, 18,025 candidates, and no `component-test` path. The complete stage contains the same 807 files and now totals 224,816,419 bytes. The redundant ignored experiment was then removed locally: 266 generated files totaling 99,606,802 bytes; it is reproducible build output.

Because PRI construction is startup-sensitive, the signed shortcut checkpoint ran early at interaction two. The exact x64 Release shortcut output rebuilt with zero warnings and errors, both authored binaries passed SignTool verification under Greg's normal current-user trust store with `CN=VaultKind Development`, and its PRI dump matched the clean stage. The shortcut launched one responsive process. Live readback reported `VaultKind Java Engine`, `%LOCALAPPDATA%\VaultKind\engine\settings.json`, and exactly locked `VK1` at `F:\VK1\VK1`. Greg confirmed the app opened and functions normally. All 67 native checks and the complete isolated disposable-vault bundled-engine probe also pass against the clean stage.

The early runtime-sensitive checkpoint passed after the Windows App SDK component change. The exact x64 Release output targeted by `VaultKind Development.lnk` was rebuilt, and its EXE and DLL both reported `Valid`, `Signature verified`, with signer `CN=VaultKind Development`. After two diagnostic launch attempts were closed normally, one clean instance reopened through the desktop shortcut, remained responsive, and displayed the healthy Dashboard with locked `VK1`. Live readback confirmed `VaultKind Java Engine`, the permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, and exactly `VK1` at `F:\VK1\VK1`. Greg confirmed normal startup and operation, then closed the app normally. All 67 native checks, the 438/145/293 class inventory, 19/30 provider inventory, twenty-three modules, direct `javaw.exe` smoke, and complete disposable-vault probe passed against the rebuilt state.

The isolated disposable-vault probe passed on July 29, 2026. It creates its vault only beneath the temporary engine profile, verifies registration, recovery-key stability, wrong-password rejection, password change, recovery-key password reset, rename, removal, reconnection, and final deregistration, then relies on the guarded wrapper to delete the complete temporary profile. The unchanged mount-provider inventory and graceful shutdown checks also pass. The 66 native policy checks pass independently. No configured user vault, user profile, provider selection, mount point, or readable drive is touched.
