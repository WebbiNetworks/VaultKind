# VaultKind 1.0 Release Readiness

VaultKind is a Windows-only, permanently English-only product. Development builds are not release candidates and must not be distributed as the only way to access important data.

## Deterministic release layout

Run `scripts/build-native-release.ps1` from a reviewed checkout to stage:

- the self-contained WinUI frontend;
- the existing Java vault-engine classes and resolved runtime dependencies;
- a minimized Java runtime created by `jlink`;
- GPL and third-party notices; and
- a machine-readable release manifest.

The native host first looks for `Engine/runtime/bin/javaw.exe`, `Engine/classes`, and `Engine/lib` beside the application. Source-tree and installed-JDK discovery remain development fallbacks and are not acceptable release dependencies.

Run `powershell -ExecutionPolicy Bypass -File scripts\test-bundled-engine.ps1 -BinaryRoot <staged-layout>` to start the staged Java runtime with an isolated profile. The probe verifies malformed-client isolation, protocol identity, an empty vault list, automatic/current mount-provider discovery, `vault_not_found` handling for all 11 vault-ID commands, unsupported-protocol and unknown-operation errors, and graceful shutdown. It does not create, connect, unlock, mount, or modify a vault or change the selected provider. This avoids reusing a compatible development engine that may already own the normal local socket.

The staging pass removes non-English .NET/WinUI satellite-resource directories. The Java engine carries only the reviewed English catalog and recovery-word list. Translation configuration, inherited localized bundles, language preferences/selectors, locale selection, and right-to-left layout machinery are absent from the product source and release.

The release classpath is resolved with Maven's runtime scope and the build fails if test-only libraries such as JUnit, Mockito, Byte Buddy, Hamcrest, Jimfs, or JavaFX Swing appear. The July 27 cleanup reduced the staged engine from 72 to 55 libraries and removed approximately 11.55 MB of test dependencies. Together with the English-only resource cleanup, the unpacked x64 stage decreased from about 283 MB to 269.69 MB while retaining a successful `backend.hello` and graceful-shutdown smoke test.

The native project references only the Windows App SDK component packages it uses: Base, Foundation, InteractiveExperiences, WinUI, DWrite, and Runtime. It does not reference the umbrella package because version 2.3.1 also declares unused Widgets, AI, and ML components. This supported component boundary removes 50,838,819 bytes from the self-contained x64 layout, including ONNX Runtime and DirectML, without filtering files after publish. The release build fails if the umbrella, Widgets, AI, ML, or Windows AI Machine Learning package returns to the restored graph, and separately checks the five reviewed Windows ML output filenames. The validated stage contains 807 files and 224,816,675 bytes; all 67 native checks and the complete bundled-engine validation remain green.

The 26,341,408-byte `Microsoft.Windows.SDK.NET.dll` remains required. VaultKind directly uses Windows Runtime APIs, and both the compiled VaultKind frontend and Microsoft's official `Microsoft.WinUI.dll` projection reference that Windows SDK projection assembly. C#/WinRT embedded projection support is not a safe replacement here because Microsoft limits it to self-contained WinRT type usage, while WinUI is a separate official interop assembly with the same dependency. This audit produced no removal and did not change the validated stage.

The PRI indexes remain Microsoft-generated build products; they are never post-filtered. MakePri inspection found that `VaultKind.Windows.pri` merges eighty-six qualified language variants for built-in WinUI control strings. Those are vendor framework and accessibility resources rather than VaultKind multilingual product content, so they remain. One ignored project-local `artifacts/component-test` output had leaked a test image path into the app PRI through WinUI's default content glob. The project now excludes `artifacts\**` during default-item evaluation. A clean full build removes only that path, reduces the app PRI by 256 bytes to 2,324,528 bytes, and leaves the validated stage at 807 files and 224,816,419 bytes. All 67 native checks and the complete isolated disposable-vault bundled-engine probe pass. The exact Release shortcut output rebuilt with zero warnings/errors, both binaries passed development-signature verification, live engine/profile/vault readback passed, and the test operator confirmed normal startup and operation.

`Microsoft.UI.Xaml.Phone.dll` and its US-English MUI remain part of the supported WinUI component deployment. Although VaultKind does not directly use their thirty-one Pivot, picker, looping-selector, automation, and navigation-transition classes, Microsoft registers the DLL as their in-process WinRT server and the generated unpackaged activation tables in VaultKind's authored binaries name it. The current Dashboard process does not load the DLL, but that single runtime state cannot clear framework-controlled activation paths. Microsoft exposes no supported Phone subcomponent opt-out, so post-filtering the 1,117,808-byte pair is prohibited.

The 1,175,704-byte `Microsoft.ui.xaml.resources.19h1.dll` and `Microsoft.ui.xaml.resources.common.dll` pair also remains. PE inspection proves both are data-only resource modules. The former carries WinUI's compiled styles and theme-resource XBF variants; the latter carries shared string-table and image resources. `Microsoft.ui.xaml.dll` explicitly names both for dynamic resource loading, so ordinary process-module absence does not make them unused. They are neither VaultKind translations nor optional satellites, and post-filtering them is prohibited.

Class-level trimming is fail-closed and incremental. The first fifty-one physical slices include exact partial slices of `org.cryptomator.ui.unlock`, `org.cryptomator.ui.health`, the direct `org.cryptomator.ui.keyloading` package, `org.cryptomator.ui.keyloading.masterkeyfile`, `org.cryptomator.ui.mainwindow`, `org.cryptomator.ui.fxapp`, `org.cryptomator.launcher`, `org.cryptomator.ui.common`, `org.cryptomator.networking`, `org.cryptomator.nativeui`, `org.cryptomator.event`, `org.cryptomator.logging`, the direct `org.cryptomator.common` package, `org.cryptomator.common.settings`, `org.cryptomator.common.mount`, and `org.cryptomator.common.vaults`, plus the exact retired `org.cryptomator.ipc` package and test-only root `JavaFXUtil`; every compiled package or partial-package union must still match its explicit reviewed set before anything is omitted. The launcher slice removes 131 retired JavaFX/IPC entry and generated-Dagger classes while retaining the exact twenty-class native/shared launcher closure. The common UI slice removes ten reviewed presentation/generated classes while retaining exactly thirty workflow and compatibility classes. The FX-application slice removes nineteen reviewed root/generated classes while retaining exactly fifteen operational compatibility classes. The main-window slice now removes thirty-one reviewed presentation/generated classes while retaining exactly twenty-seven functional diagnostic, controller, module, navigation, scene, vault-detail, context-menu, list, welcome, and workflow classes. The health slice now removes twenty-four reviewed presentation/generated classes while retaining exactly thirty-three functional controller, execution, reporting, fixing, encrypted-vault health/repair, and module/provider classes. The direct key-loading slice removes one generated FXML-loader wrapper while retaining exactly eight functional component, module, scope, strategy, and strategy-provider classes. The masterkey-file slice removes eight generated controller, scene, and result-holder wrappers while retaining exactly fifteen functional controller, component, module, result, stored-password, loading-strategy, password-entry, and masterkey-handling classes; its saved-password and loading-strategy factories are explicitly protected as security-functional. The Windows-only networking slice removes four macOS/Linux trust-store classes while retaining the seven-class Windows TLS/provider family for future Hub HTTPS compatibility. The separate nine-class legacy IPC stack is removed only after proving it has no staged inbound edge and is unrelated to the retained native socket bridge. The test-only JavaFX platform-startup helper remains in source but not the native package. Nine unused generated native factories are omitted while all twenty-one functional native classes remain; four unused generated event factories are omitted while all nine functional event classes remain; the unused generated debug-mode factory is omitted while all six functional logging classes remain; seven unused generated direct-common wrappers are omitted while functional licensing, executor creation, reveal-path provider discovery, and thirty-one direct common classes remain; three unused generated settings factories are omitted while all sixteen functional settings classes remain; five unused generated mount wrappers are omitted while native construction, functional mount behavior, Windows drive-letter handling, and both required mount-service collection factories remain; nine unused generated vault wrappers are omitted while all thirty-eight functional and native-required registry, persistence, state, statistics, snapshot, lifecycle, exception, and auto-lock classes remain. The exact runtime-artifact filters remove jlink's two Windows linker import libraries, five unused administration/diagnostics/accessibility launchers, the Japanese-only PostScript printer mapping, the JDK-8 external-tooling copy of the `jrt` filesystem provider, two unused Flight Recorder templates, the unused Java Sound provider-override configuration, the opt-in strict-JAXP compatibility-testing template, and the orphaned CDS dump-time class list from this archive-free image; they retain both `java.exe` and the native host's required `javaw.exe`, preserve the built-in Java 26 `jrt` provider, all three JFR/management modules, `java.desktop` and native audio support, VaultKind's independently packaged signature sounds, active JAXP configuration and XML security limits, the complete `java.xml` module, general Java 2D/font configuration, and all required runtime configuration/security/legal files, and fail closed if any reviewed set changes. The complete-stage hash audit additionally removes only the unused engine-resource duplicate of `THIRD-PARTY.txt` while requiring the identical generated notice to remain published under `Notices`; all per-module Java legal and security-policy copies remain. The July 30 filtered stage retains all 145 statically reachable authored classes, passes all 67 native checks, complete 23-module image enumeration, archive-free CDS diagnostics, a direct hidden `javaw.exe` smoke, and the complete disposable-vault protocol probe through `java.exe`, and has decreased by exactly 4,207,227 bytes to 275,655,494 bytes. Reachable event aggregation, notification interfaces, reveal-path integrations, encrypted-vault health/repair behavior, functional native and legacy settings, startup, diagnostics/logging, key-loading, keychain, vault unlock, mount-provider services, all seventy Hub key-loading classes, all seven common keychain classes, the Windows TLS provider, functional licensing, neutral vault creation and recovery primitives, and all twenty-five vault-format migration classes remain packaged. Migration and the dynamically loaded tray-menu provider remain protected. Broad wildcard removal and third-party JAR filtering remain prohibited.

The staged-engine probe now creates an encrypted vault only beneath its guarded temporary profile. It validates creation and registration, recovery-key display, wrong-password rejection, password change, recovery-key password reset, rename, removal, reconnection, final deregistration, and temporary-profile cleanup. It does not select a provider, mount a readable drive, or touch a configured user vault. This closes the disposable-vault evidence gap for those unmounted operations; mounted/provider-specific behavior remains a separate release gate.

## Version 1.0 distribution boundary

VaultKind will not depend on a recurring commercial code-signing subscription. The primary candidate for version 1.0 is a Microsoft Store MSIX: Microsoft accepts an unsigned Store upload, validates it, and signs the package after certification. The isolated WinUI 3 proof app, **VaultKind Store Path Test**, passed certification and entered the Microsoft Store on July 28, 2026. The Store-signed version 1.0.0.0 package installed successfully, moved through Windows' supported app-volume mechanism to `G:\WindowsApps`, and launched its expected WinUI window under the Microsoft-assigned package identity `Webbi.VaultKindStorePathTest_1014d67w6rsqa`. Version 1.0.1.0 then passed the normal Store update, uninstall, clean reinstall, and post-reinstall launch paths on July 31. This proves the account, upload, certification, signing, acquisition, installation, app-volume relocation, update, uninstall, reinstall, and launch paths for the isolated packaging approach. It does not yet prove VaultKind's larger package, bundled Java engine, virtual-drive integrations, or retention of external encrypted vault data.

The fallback is an unsigned portable Windows archive. Windows, SmartScreen, endpoint protection, or organization policy may warn about or block unsigned executables. Any portable download page and release notes must state that plainly and publish a SHA-256 checksum. VaultKind must never ask users to disable security controls globally; users whose policy blocks the app may be unable to run that build.

The Store update proof is version `1.0.1.0` of **VaultKind Store Path Test**. Its window reads the installed package version directly from `Package.Current.Id.Version`, so Store-managed replacement of installed version `1.0.0.0` can be distinguished from a reinstall. The validated unsigned upload is `StorePathProof_1.0.1.0_x64.msixupload`, 54,691,507 bytes, with SHA-256 `BC4254C9BE56598A62D9C19589DB090AB47358A8C6AACC6737B8CB251544B4EB`. Microsoft certified Submission 2 (`1152921505701528725`) on July 31, 2026 with the original pricing, availability, properties, age rating, listings, and publishing settings unchanged. The installed `1.0.0.0` package updated through Microsoft Store and launched from `G:\WindowsApps` with the expected `1.0.1.0` readout. A subsequent uninstall removed the test package, the Store offered a clean install, and the reinstalled package launched from the default `C:\Program Files\WindowsApps` volume with the same `1.0.1.0` identity and readout. The isolated Store distribution experiment is therefore complete; the real-product engine, provider, and external-vault retention gates remain open.

The real **VaultKind** MSIX/PWA product name was reserved in Partner Center on July 31, 2026. Its reviewed identity source is `packaging/store-identity.json`: Store ID `9P31PF0927Z4`, package name `Webbi.VaultKind`, publisher `CN=B46E8F20-201E-4AEB-AF2B-B6AB3D44E5FC`, publisher display name `Webbi`, and package family `Webbi.VaultKind_1014d67w6rsqa`. `scripts/build-vaultkind-store-upload.ps1` validates those exact values and produces the unsigned `.msixupload` that Microsoft will certify and sign at no recurring certificate cost. The Store package carries profile ID `Store` with `developmentOnly: false`, preventing it from attaching to the unpackaged or `.Development` profiles. A fake-identity dry run produced one unsigned x64 MSIX inside one `.msixupload`, with the exact false-valued marker and no `AppxSignature.p7x`; the disposable artifact was then removed. No real VaultKind submission has been started.

The runtime-sensitive production-marker checkpoint passed before this pipeline was committed. The exact x64 Release shortcut target rebuilt with zero warnings/errors and its authored EXE/DLL were signed with the local development certificate. The test operator launched it manually; the expected first-post-rebuild validation path took approximately eleven seconds, then the Dashboard and normal functions were correct. Read-only inspection confirmed `VaultKind Java Engine`, permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json`, locked `VK1` at `F:\VK1\VK1`, locked `VK2` at `F:\Test1\VK2`, Automatic, and all five providers. This proves the new Store-marker support did not alter the marker-free unpackaged profile.

The first real-identity Store upload build passed local package acceptance on July 31, 2026. WACK exposed four oversized unqualified artwork aliases even though its overall result was PASS. The packaging pipeline now carries genuine 100%- and 200%-scale variants, validates all eight dimensions before MakeAppx, and removes a stale checksum whenever replacing an artifact. The corrected `artifacts\VaultKind-1.0.0.0-win-x64.msixupload` is 96,469,603 bytes with SHA-256 `0E4F25149DA6B04B5A9CF42045EB926E5B8834920D2AB20EE25071DE8CF983F7`. Its sole payload is `VaultKind-1.0.0.0-win-x64.msix`, a 97,192,429-byte x64 MSIX with SHA-256 `0D1A6295F8CAAAC6F6EC72C577AE9417E9EB8861375DA11560B9EB5E10464D65`. The inner package has 800 entries and the exact identity `Webbi.VaultKind`, publisher `CN=B46E8F20-201E-4AEB-AF2B-B6AB3D44E5FC`, publisher display name `Webbi`, version `1.0.0.0`, and architecture `x64`. Its marker is exactly `{"profileId":"Store","packageName":"Webbi.VaultKind","developmentOnly":false}`. It contains the Windows executable, release manifest, bundled engine libraries/classes/runtime, Java runtime legal files, VaultKind notices, and the complete approved icon family. `AppxSignature.p7x` is absent as required because Microsoft signs the package only after certification.

Windows App Certification Kit `10.0.26100.7705` completed all 24 tests against the corrected inner package and returned overall `PASS` with `PARTIAL_RUN="FALSE"`. The App resources test now passes. Twenty-two tests pass directly. The two remaining failed tests are optional: General metadata correctness reports WinUI/WebView metadata supplied by Microsoft runtime components, and Blocked executables reports VaultKind's required Java process launch plus analyzer substring matches throughout Microsoft DLLs, Java libraries, resources, and even `AppIcon.ico`. These findings must be disclosed in certification notes; removing the real process-launch path would prevent the native shell from starting its bundled encryption engine. The production artifact has not been installed, uploaded, or submitted.

### Rule: local testing with the real Microsoft Store identity

The production `.msixupload` must remain unsigned and must never be modified for local testing. Make a separately named local-test package from an isolated copy of its staged content. Use a disposable, non-exportable code-signing certificate whose subject exactly matches the Microsoft-assigned publisher, `CN=B46E8F20-201E-4AEB-AF2B-B6AB3D44E5FC`. Sign the authored `VaultKind.Windows.exe` and `VaultKind.Windows.dll` before packing, pack the local-test MSIX, and sign the MSIX envelope. On this machine, use the known-working SignTool at `%USERPROFILE%\.nuget\packages\microsoft.windows.sdk.buildtools\10.0.28000.2270\bin\10.0.28000.0\x64\signtool.exe`; Windows Application Firewall blocked the SDK 10.0.26100 SignTool during this proof.

For installation, temporarily add only the matching public certificate to `LocalMachine\TrustedPeople`, which requires the test operator's explicit approval and a UAC prompt. In the same elevated operation, validate the exact thumbprint, install the signed local-test MSIX with `Add-AppxPackage`, verify package family `Webbi.VaultKind_1014d67w6rsqa` and the expected version, launch `Webbi.VaultKind_1014d67w6rsqa!App`, and remove that exact certificate in a `finally` block whether installation succeeds or fails. Verify afterward that the certificate is absent from both Current User and Local Machine stores. Current User `Root`/`TrustedPublisher` trust is insufficient for AppX deployment and fails with `0x800B0109`. Repacking with no signature and using `Add-AppxPackage -AllowUnsigned` is also invalid for this proof because Windows accepts that mode only for the `CN=Unsigned` namespace and rejects the real Store publisher with `0x80073D2C`. Do not retry either failed route. Never upload the signed local-test copy; Microsoft must sign the untouched production upload after certification.

The real-identity local runtime proof passed on August 1, 2026. The signed test copy installed as `Webbi.VaultKind_1.0.0.0_x64__1014d67w6rsqa`, status `Ok`, launched its native shell and packaged Java engine from `C:\Program Files\WindowsApps`, and used the isolated logical profile `%LOCALAPPDATA%\VKP\Store\VaultKind\engine\settings.json`; Windows virtualized the physical files beneath the package's `LocalCache`. Initial and final protocol inspection both reported zero vaults, Automatic, and all five providers. The guarded package probe explicitly selected Windows Explorer WebDAV, created one disposable encrypted vault only beneath `VaultKind-Msix-Probe-*` in Windows Temp, unlocked it to a new drive letter, wrote/read/deleted one marker, locked and unmounted, deregistered the vault, restored Automatic and the exact `ProxyOverride` state, removed the temporary fixture, and left the package engine healthy. The test operator then closed the app normally. Exact-name uninstall removed only `Webbi.VaultKind_1.0.0.0_x64__1014d67w6rsqa`; package data and Store processes were absent, and the empty external Store runtime directory plus generated ignored local-test artifacts were removed. The test certificate was absent from all checked Current User and Local Machine stores after installation and throughout the probe. The untouched production upload remained 96,469,603 bytes with SHA-256 `0E4F25149DA6B04B5A9CF42045EB926E5B8834920D2AB20EE25071DE8CF983F7`. This closes the real Store-identity local install/launch/engine/WebDAV/uninstall gate; it does not authorize uploading or submitting the production artifact.

The public-repository cleanup checkpoint passed on August 1, 2026. Git whitespace, Maven-model, and PowerShell syntax checks passed; all 315 Java tests passed with two expected platform skips when the scanner-sensitive temporary directory was placed inside the excluded workspace; and all 87 native checks passed. The exact x64 Release shortcut target rebuilt with zero warnings/errors, and both authored binaries verified with valid `CN=VaultKind Development` signatures. The test operator launched the desktop shortcut, reached the Dashboard in approximately sixteen seconds, confirmed the expected configured vaults and normal functions, and closed the app normally. This validates the repository and build-profile cleanup; it does not replace the frozen release-candidate accessibility or Store-submission gates.

The mandatory local-development checkpoint passed after the package-art correction. All 87 native checks pass. The exact x64 Release target used by `VaultKind Development.lnk` rebuilt with zero warnings/errors; its authored EXE and DLL both have valid `CN=VaultKind Development` signatures. The test operator launched it from the desktop shortcut, reached the Dashboard in approximately eighteen seconds, and confirmed normal functions. Read-only protocol inspection reported `VaultKind Java Engine`, permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json`, locked `VK1` at `F:\VK1\VK1`, locked `VK2` at `F:\Test1\VK2`, Automatic, and all five providers.

The August 1 dependency-maintenance checkpoint updated the shared Jackson runtime from 2.21.4 to 2.21.5. All 315 Java tests passed with two expected platform skips when temporary test files were rooted inside the excluded workspace, all 87 native checks passed, and the isolated bundled-engine lifecycle passed with `jackson-core`, `jackson-databind`, and `jackson-datatype-jsr310` 2.21.5 staged. The exact x64 Release shortcut target rebuilt with zero warnings/errors and its EXE/DLL both carried valid `CN=VaultKind Development` signatures. The first post-build launch took more than ten seconds and the immediate second launch was effectively instant, consistent with the established cold security-validation pattern. The test operator confirmed normal operation and created a disposable vault through the occupied-parent path, which correctly created a named child folder. Read-only running-state evidence showed the responsive signed frontend, responsive Java engine, permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, Automatic selection, and four configured vault registrations including the new `DPCable` vault at `F:\MooseT\DPCable`.

The virtual-drive Store boundary is now explicit. The staged engine discovers a Windows WebDAV provider family and a WinFsp-based FUSE provider family; VaultKind's MSIX must not attempt to install either a driver or a user service. Microsoft documents that MSIX does not support Windows drivers, while Store policy 10.2.4 generally disallows dependency on non-Microsoft drivers or NT services except case-by-case consideration for WHCP-certified drivers, with disclosure in both listing metadata and certification notes. Therefore the Store build must first prove a useful vault workflow with the Microsoft-provided Windows/WebDAV path and treat WinFsp as an optional preinstalled enhancement unless Microsoft explicitly approves the disclosed dependency. Before the real submission, run a packaged disposable-vault create, unlock, readable-drive I/O, lock, update, and uninstall/reinstall matrix without changing any configured user vault.

The July 31 live-protocol provider audit reported four concrete providers plus the synthetic Automatic choice. `org.cryptomator.frontend.webdav.mount.WindowsMounter` is **WebDAV (Windows Explorer)** and reports drive-letter plus loopback-port support; it is the first Store-package candidate. `org.cryptomator.frontend.webdav.mount.FallbackMounter` is **WebDAV (HTTP Address)** and reports only a loopback port, so it cannot satisfy the readable-drive test. `org.cryptomator.frontend.fuse.mount.WinFspNetworkMountProvider` and `org.cryptomator.frontend.fuse.mount.WinFspMountProvider` expose the WinFsp network/local-drive choices and must remain optional preinstalled enhancements. Automatic is not itself a provider: it advertises the capability union while the current selector resolves a null selection to the first discovered concrete service. On this machine that order begins with WinFsp, so the Store test must explicitly select `WindowsMounter` in its isolated profile rather than rely on Automatic. The read-only audit left both configured vaults locked and unchanged.

The opt-in mounted probe is `scripts/test-bundled-engine.ps1 -BinaryRoot <staged-layout> -MountedWebDav`. It refuses non-artifact inputs, creates its engine profile and encrypted fixture only beneath a fresh Windows temporary directory, requires an initially empty registry, explicitly selects and reads back `WindowsMounter`, and accepts only a newly created drive-letter root that did not exist before unlock. It writes, reads, verifies, and deletes one uniquely marked test file, then locks, waits for drive removal, deregisters the vault, restores Automatic inside the isolated profile, requests shutdown, and deletes the temporary tree. Failure cleanup attempts file removal, lock, deregistration, setting restoration, and shutdown before the wrapper may terminate the isolated process. Because the upstream adapter adds localhost entries to the current user's Internet Settings `ProxyOverride` value and does not undo them on unmount, the wrapper snapshots that exact value and restores it in `finally`; a restoration failure fails the probe. The test must run in the normal user context because sandboxed registry and `net use` operations are intentionally denied.

The first normal-user mounted run passed on July 31, 2026. It proved disposable create/unlock, a new Windows Explorer WebDAV drive, cleartext file write/read/delete, lock, unmount, deregistration, isolated setting restoration, engine shutdown, and temporary-directory deletion. After the upstream adapter's persistent proxy-bypass behavior was identified, repeated guarded runs additionally confirmed exact before/after `ProxyOverride` restoration. No network mapping or test directory remained after any run. Windows started the existing manual `WebClient` service during the mount and left it running; the probe deliberately does not stop a shared Windows service that another process may begin using. This establishes the unpackaged staged engine's built-in Windows/WebDAV workflow, but it does not yet establish the same workflow from a Store-packaged real VaultKind build or external encrypted-data retention across Store update/uninstall/reinstall.

Create the portable archive and checksum with `scripts/build-native-release.ps1 -CreatePortableArchive`. An unsigned MSIX downloaded directly from the project is not a usable substitute: Windows requires an MSIX package to be signed before normal installation. The Store route is different because the Store signs the accepted package; direct signed distribution remains unavailable without a separately controlled signing identity.

The release script retains optional certificate parameters for a future signed release. `scripts/build-native-msix.ps1` also remains available for local development validation or that future milestone; it verifies that the manifest publisher exactly matches the signing certificate subject.

For local development on a Windows Application Control-protected workstation:

1. Run `powershell -ExecutionPolicy Bypass -File scripts\setup-native-development-signing.ps1` once. This creates a non-exportable current-user code-signing key and adds only its public certificate to the current user's Root and TrustedPublisher stores.
2. Build VaultKind.
3. Run `powershell -ExecutionPolicy Bypass -File scripts\sign-native-development.ps1 -BinaryRoot <build-output-directory>` after every build.

The development certificate must never be used for distributed builds. It exists only to let the current protected workstation run and test local builds.

Development and packaged frontends use the same persistent engine profile at `%LOCALAPPDATA%\VaultKind\engine`; source-tree execution must never place `settings.json`, logs, plugins, or mount-point state under Maven's disposable `target` tree. This boundary was enforced after a July 29 development rebuild erased `target\ui-dev-profile` and therefore lost the local registration list without deleting any encrypted vault data. The rebuilt and development-signed shortcut build now starts `NativeBackendMain`, reports the permanent profile, and reads the restored `VK1` registration at `F:\VK1\VK1`. The Diagnostics-folder action opens the corresponding persistent engine log directory. A guarded test-harness command can inspect the live engine or restore one validated vault registration only when the current list is empty; it refuses to merge into or overwrite a nonempty list. All 67 native checks pass after this correction.

During active editing, the exact development shortcut output must be rebuilt and development-signed at least every ten user/assistant work interactions, closed and reopened normally through the shortcut, and checked for the expected backend identity, persistent profile, and vault list. Perform this checkpoint sooner after runtime-risk changes. Once every checkpoint step passes, commit the accumulated tracked work and push the current branch before resetting the interaction count.

During active code editing, complete a real local-build checkpoint at least once every ten user/assistant work interactions, and sooner after runtime-risk changes or before a commit. A checkpoint means rebuilding the exact Release output targeted by `VaultKind Development.lnk`, signing its executable and DLL, closing and reopening it normally, and verifying the live backend identity, permanent profile, and vault list. Reset the interaction counter only after the complete checkpoint passes.

The July 30 checkpoint passed after the fifty-first class slice and second runtime-artifact slice. The exact shortcut target was fully rebuilt; both local development signatures verified as valid; the app closed normally, reopened through the desktop shortcut, and remained responsive; live readback confirmed `VaultKind Java Engine`, the permanent profile, and locked `VK1` at `F:\VK1\VK1`; the test operator confirmed normal operation. All 67 native checks, the 438/145/293 class inventory, the unchanged 19-descriptor/30-provider inventory, direct `javaw.exe` smoke, and complete disposable-vault probe passed.

The July 31 inventory refresh initially found no boundary drift, then the fifty-second exact class slice removed only six 7,456-byte Dagger-generated Vault Doctor reference/property factory wrappers. Staged bytecode has no inbound edge to them and their sole generated application-graph consumer is the already omitted legacy JavaFX root. `HealthCheckModule`, its underlying provider methods, and all functional health/repair implementations remain. The current 801-file stage is 224,808,963 bytes with 432/145/287 authored/reachable/candidate classes. All 55 release JARs remain whole; the 19-descriptor/30-provider inventory, dynamic-retention inventory, 67 native checks, and complete isolated disposable-vault lifecycle probe remain green.

The fifty-third exact class slice removes three more generated Vault Doctor provider wrappers totaling 5,842 bytes. They also have no staged inbound edge and are consumed only by the omitted legacy JavaFX application graph. Their underlying available-check and key-loading-strategy provider methods remain in `HealthCheckModule`, alongside every functional health/repair implementation. The current 798-file stage is 224,803,121 bytes with a 429/145/284 class boundary and cumulative savings of 55,059,600 bytes. Provider/dynamic inventories, all 67 native checks, and the complete isolated lifecycle probe remain green.

The fifty-fourth exact class slice removes only the `HealthCheckComponent` and nested `Builder` Dagger declarations, totaling 3,254 bytes. Staged bytecode has no inbound edge to them, and their only generated consumers belong to the already omitted legacy application and JavaFX vault-options graphs. The functional module and every health/repair implementation remain. The current 796-file stage is 224,799,867 bytes with a 427/145/282 class boundary and cumulative savings of 55,062,854 bytes. Provider/dynamic inventories, all 67 native checks, and the complete isolated lifecycle probe remain green.

The next main-window declaration proposal was retained after the fail-closed audit found staged inbound links from `FxApplicationWindows` and `MainWindowModule`. The wider dependency closure also crosses retained Vault Doctor, Hub, migration, tray, and main-window workflow code. No class or byte was removed by this audit; the validated stage remains unchanged.

The fifty-fifth exact class slice removes only the disconnected JavaFX `AutoUnlocker` and `FxNotificationManager` implementations, totaling 11,575 bytes. Both have zero staged inbound edges and are constructed only by the already omitted legacy Dagger/JavaFX root. VaultKind's WinUI startup preference explicitly keeps vaults locked, while core event/notification interfaces and every native unlock workflow remain. The current 794-file stage is 224,788,292 bytes with a 425/145/280 class boundary and cumulative savings of 55,074,429 bytes. Provider/dynamic inventories, all 67 native checks, and the complete isolated lifecycle probe remain green.

The fifty-sixth exact class slice removes only the disconnected JavaFX `AppLaunchEventHandler` and its `$1` switch-map companion, totaling 8,646 bytes. Their only staged inbound links are mutual; the event type, legacy IPC/file-open producers, JavaFX application root, generated factory, and generated graph consumers are already omitted. VaultKind's WinUI executable has an independent `App.OnLaunched` startup path and currently declares no supported protocol or file-type activation. The native socket bridge and all protocol, profile, startup, and native picker behavior remain. The current 792-file stage is 224,779,646 bytes with a 423/145/278 class boundary and cumulative savings of 55,083,075 bytes. Provider/dynamic inventories, all 67 native checks, and the complete isolated lifecycle probe remain green.

The next 2,586-byte FX compatibility proposal was retained. Fresh staged bytecode links `ExitingQuitResponse` from the retained shutdown coordinator, `FxApplicationScoped` from retained shutdown/window/filesystem-event and common/main-window workflows, and `PrimaryStage` from retained window, main-window, and migration workflows. Removing their consumer closure would cross protected functional boundaries. No class, output, or byte changed; the validated 792-file stage remains 224,779,646 bytes with the 423/145/278 boundary. Broad class trimming should now pause unless a new exact, disconnected seam is proven; remaining release work has higher value than speculative byte recovery.

The following mandatory signed-shortcut checkpoint passed. The exact Release shortcut target rebuilt with zero warnings and errors, and SignTool verified both authored binaries with `CN=VaultKind Development` under the test operator's normal user trust store. Launching through the existing desktop shortcut produced one responsive VaultKind window with the healthy Dashboard and locked `VK1`. Read-only live inspection confirmed `VaultKind Java Engine`, the permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, and exactly locked `VK1` at `F:\VK1\VK1`. The test operator confirmed normal operation and closed the app normally. The automated class/provider, native, and isolated lifecycle evidence remains green.

A second July 30 checkpoint passed immediately after replacing the Windows App SDK umbrella dependency with the exact component packages VaultKind uses. The exact shortcut output was fully rebuilt and both authored binaries verified with valid development signatures. One clean instance reopened through `VaultKind Development.lnk`, remained responsive, showed the healthy Dashboard and locked `VK1`, and reported the expected live engine identity, permanent profile, and single vault registration. The test operator confirmed normal startup and operation and closed it normally. All automated native, class/provider, Java-runtime, and disposable-vault checks also passed.

For local package-pipeline validation only, use a separate identity ending in `.Development`:

```powershell
powershell -ExecutionPolicy Bypass -File scripts\build-native-msix.ps1 `
  -BinaryRoot artifacts\VaultKind-1.0.0-win-x64 `
  -PackageName WebbiNetworks.VaultKind.Development `
  -Publisher "CN=VaultKind Development" `
  -SigningThumbprint <development-certificate-thumbprint> `
  -IsolatedProfileId LocalMsix.WebDav `
  -DevelopmentPackage
```

The script refuses to use `CN=VaultKind Development` for the production identity. Every development package must use a `.Development` package name and an isolated profile identifier. The builder injects a development-only marker into signed package content. Engine settings and every auxiliary native UI store are rooted beneath `%LOCALAPPDATA%\VKP\<id>` and isolated by Windows package virtualization; the Java child receives the same logical data root through its process-local `LOCALAPPDATA`. The ephemeral local bridge alone uses the owner-only `%USERPROFILE%\.vaultkind-runtime\<id>\native-bridge-v1.sock` path so Java and .NET agree on one short physical endpoint outside the virtualized data tree. Identifiers are limited to sixteen characters and the complete Unix-domain-socket path is rejected if it exceeds Windows' 108-character limit. The package cannot reuse the permanent unpackaged profile. Production packaging rejects this option, and staged release input is rejected if it already contains a marker. A package produced this way is not the version 1.0 distribution artifact.

The first isolated development package build passed on July 31, 2026 without installation. `VaultKind-1.0.0.0-win-x64-development.msix` contains 798 files and is 96,202,604 bytes with SHA-256 `82B79BE6341FF97AC3F8618B22DE05EE3045D090A074BDE648B236AEB6CD20EE`. Direct archive inspection confirmed identity `WebbiNetworks.VaultKind.Development`, publisher `CN=VaultKind Development`, version `1.0.0.0`, x64 architecture, full-trust `VaultKind.Windows.exe`, and marker `{"profileId":"LocalMsix.WebDav","packageName":"WebbiNetworks.VaultKind.Development","developmentOnly":true}`. SignTool verified the package with zero warnings and errors. The portable staged input remains marker-free. Immediately beforehand, the signed unpackaged shortcut build opened normally against the permanent profile and read-only inspection found locked `VK1` at `F:\VK1\VK1` and locked `VK2` at `F:\Test1\VK2`; the test operator confirmed normal operation. This proves package construction and the static isolation boundary, not installed-package execution or mounted-vault behavior.

The first installed launch activated the separate package identity and showed zero vaults, proving it did not read the permanent VK1/VK2 registry. Version `1.0.0.0` exposed a 109-character bridge path, one beyond Windows' limit. Version `1.0.0.1` shortened the logical path, but MSIX file-system virtualization expanded its physical location and made Java/.NET endpoint resolution unsuitable. VaultKind deliberately avoids the restricted `unvirtualizedResources` capability: package data stays virtualized while only the ephemeral local bridge moves to the short owner-only external runtime path.

Development package `VaultKind-1.0.0.2-win-x64-development.msix` is 96,203,661 bytes with SHA-256 `698612802D2764E3CF5FFD90BDBECE134AD3AF738AB032769463C54FC2088F07`; SignTool and Windows both report its `CN=VaultKind Development` signature as valid. Its installed window reached a healthy engine in 3.75 seconds and showed zero vaults with fresh package-local auxiliary state. Direct protocol inspection confirmed `VaultKind Java Engine`, logical profile `%LOCALAPPDATA%\VKP\LocalMsix.WebDav\VaultKind\engine\settings.json`, zero vaults, and all five provider choices. The guarded package probe explicitly selected `WindowsMounter`, created and unlocked one temporary external encrypted vault, mounted a new drive letter, wrote/read/verified/deleted a marker, locked and unmounted, deregistered the vault, restored Automatic, deleted the guarded fixture root, and left the package engine running. This closes local packaged launch and Windows Explorer WebDAV I/O; it does not yet prove Store-certified update/uninstall/reinstall retention of external encrypted vault data.

The local development-package retention matrix is now complete. A disposable external `Mooselock` vault was created and exercised through the packaged UI. Its encrypted storage fingerprint was recorded as eight files, 2,403 bytes, manifest SHA-256 `EBB8C4F49AD19F4B2CA1AFDAD2884922F0E650F63079E522DDC0F37CA09BCAC0`. That fingerprint and the registered locked vault survived an in-place package update unchanged. Uninstalling only `WebbiNetworks.VaultKind.Development` removed package-local state while leaving the external encrypted folder exact. A clean reinstall correctly began with an empty package registry; reconnecting the preserved folder, unlocking it, reading its disposable cleartext file, and locking it again all passed. This closes the local real-app MSIX update/uninstall/reinstall/external-data boundary. It does not replace certification, install, or update testing under VaultKind's future Microsoft-assigned production Store identity.

`VaultKind-1.0.0.6-win-x64-development.msix` is 96,208,026 bytes with SHA-256 `C6D5477EA80E5863481F44D205D8207A0D10EBA52D24458989A3D13DF6A08D89`. Its package envelope and both authored internal binaries pass development-signature verification, and Windows reports installed identity `WebbiNetworks.VaultKind.Development_1.0.0.6_x64__w744d62bxd7e0` as healthy. Development-package construction now signs and verifies the staged EXE and DLL before MakeAppx, then signs and verifies the MSIX. This hardening follows version `1.0.0.3`, whose valid envelope still failed Windows Application Control because its refreshed internal binaries were unsigned; the test operator unblocked the local WAF event, and corrected versions passed.

The same package introduced a safer vault-location policy, subsequently tightened after an empty folder with a different name still produced unwanted nesting. Any explicitly selected empty folder is now used exactly as selected, regardless of the vault's display name. Selecting a non-empty parent creates the named child; selecting a matching non-empty folder is rejected instead of silently producing `Name\\Name`. The wizard displays the exact resolved path before creation and freezes both the validated display name and physical path on entry to Review; the final Create action no longer depends on hidden earlier-step controls. The create protocol carries the requested display name independently, the engine validates and applies it after registration, and validation failures distinguish path, display-name, and password errors. The isolated lifecycle probe requires both the requested name and exact expanded physical path. Connect-vault unsupported-folder messages wrap completely rather than being clipped. The native regression suite covers the exact-empty-folder, non-empty-parent, occupied-match, existing-child, and name/path-separation boundaries.

The mandatory unpackaged checkpoint immediately afterward rebuilt the exact `VaultKind Development.lnk` x64 Release target with zero warnings/errors and signed both authored binaries successfully. One normal shortcut instance opened responsively with the permanent profile, green engine status, and locked `VK1` at `F:\VK1\VK1` plus locked `VK2` at `F:\Test1\VK2`. Direct protocol inspection confirmed the permanent profile and all five providers; the test operator confirmed the normal app looked and functioned correctly.

The post-`1.0.0.6` checkpoint rebuilt that same exact shortcut target with zero warnings and errors and verified valid development signatures on its EXE and DLL. The test operator launched it manually after closing the packaged app; startup took approximately eleven seconds, both configured vaults appeared, and the app functioned normally. Read-only inspection confirmed `VaultKind Java Engine`, permanent profile `%LOCALAPPDATA%\VaultKind\engine\settings.json`, locked `VK1` at `F:\VK1\VK1`, locked `VK2` at `F:\Test1\VK2`, Automatic, and all five providers. The startup observation is retained for later performance comparison; it did not block this correctness checkpoint.

The placeholder Windows asset set has been replaced with a coherent VaultKind family derived from the already approved brand artwork. The transparent metallic-and-blue VK mark now supplies Store, square, small/unplated, lock-screen, executable, desktop-shortcut, title-bar, and taskbar icons; the full lockup supplies only the wide tile and splash screen. The executable embeds an eight-resolution ICO (16, 20, 24, 32, 48, 64, 128, and 256 pixels), and the package PNGs are normalized to 8-bit at their required dimensions. The exact x64 Release target rebuilt with zero warnings/errors, its authored binaries were signed, all 78 native checks passed, and live inspection again found the permanent profile with locked VK1/VK2 and all five providers. The test operator confirmed all three immediately visible icon surfaces: desktop shortcut, title bar, and running taskbar. The first launch of each newly rebuilt and re-signed binary took approximately eleven seconds from click to Dashboard, while the privacy-safe in-app trace reached the ready Dashboard in 3.58 seconds. An immediate unchanged relaunch took approximately two seconds end to end. This isolates the longer observation to the new-binary cold/security-validation path rather than a persistent VaultKind startup regression.

## Remaining release gates

- Rebuild and inspect the real-identity VaultKind `.msixupload` from the frozen release commit, complete final package/runtime/accessibility checks, then submit it for certification only with Greg's explicit approval. After certification, repeat acquisition, install, update, relocation, uninstall, reinstall, and external-vault retention testing under the production identity.
- Complete the prepared Partner Center fields, listing media, public privacy/support destinations, restricted-capability explanation, certification notes, and evidence checklist in [STORE_SUBMISSION_PREPARATION.md](STORE_SUBMISSION_PREPARATION.md).
- If the Store route fails or is unsuitable, produce the unsigned portable ZIP and publish its SHA-256 checksum.
- Verify first-run extraction, replacement/upgrade behavior, removal behavior, and retained user data.
- Test extraction and first launch on clean supported Windows virtual machines with no JDK, Maven repository, source checkout, or Developer Mode.
- Test x64 first; ship ARM64 only after equivalent engine, virtual-drive, and native-library validation.
- Run native automated tests and the existing Java test suite from the exact release commit.
- Verify unlock, readable-drive reveal, lock, shutdown protection, recovery, and upgrade against disposable test vaults.
- Confirm the app is understandable and operable with signature sounds disabled.
- Complete keyboard-only, Narrator, Windows text-scaling, high-DPI, and minimum-window accessibility passes on the release build.
- Record checksums and retain the exact artifacts used for release.
- Rebuild portable ZIP and Store-upload artifacts from the frozen release commit; superseded local packages must not be retained as release candidates.

## Current accessibility baseline

The native shell uses visible system focus indicators, moves keyboard focus into the workspace after navigation, and gives icon-only password-reveal controls explicit accessible names. The sidebar supports Up/Down movement plus Home/End jumps across Dashboard, Vault Doctor, the prominent Add Vault primary action, Vault Manager, configured vaults, Activity, Preferences, and Learning Center; Tab and Enter behavior remains available. Vault Manager opens a neutral all-vault list and does not choose the first vault automatically. Each vault can then be opened deliberately, and the management detail provides a configured-vault selector. The complete implemented key map and manual test matrix are maintained in [KEYBOARD_CONTROLS.md](KEYBOARD_CONTROLS.md). Dynamic workflow status text is exposed as a polite UI Automation live region, with a `LiveRegionChanged` event raised after a visible message changes. Busy indicators have task-specific names such as Opening vault, Creating vault, and Password recovery in progress rather than an unlabeled generic progress control.

On the test operator's current keyboard, WinUI's attached `ContextFlyout`, explicit `KeyboardAccelerator`, routed-key, and content-island context-menu paths did not produce a usable `Shift+F10` invocation. The discarded accelerator approach also produced unwanted shortcut tooltips. Those failed workarounds are not shipped. Each sidebar vault instead exposes a visible, explicitly named **More actions** button. It is reachable with ordinary `Tab` navigation and opens with `Enter` or `Space`, so every context action has a confirmed single-key activation path after focus reaches the button. The test operator confirmed the complete path on the signed Release build.

The static accessible-name and focus audit covers both fixed XAML controls and controls generated at runtime. Stateful Preferences, Learning Center, FAQ, and Assistant controls now expose their selected, viewed, expanded, collapsed, or case context in their accessible names. Navigation and replacement flows deliberately place or restore focus across primary workspaces, vault creation, management, sharing, statistics, file tools, FAQ filtering, Assistant results, and completion states.

These semantic foundations do not replace hands-on assistive-technology testing. Before 1.0, test the exact release build with Narrator and keyboard-only input across creation, connection, unlock, lock, recovery, password change, removal, Vault Doctor, Preferences, Learning Center, and Assistant. Repeat the core flows at increased Windows text scale, supported DPI settings, and the minimum usable window size; record every blocker and retest its fix.

On July 27, 2026, the current signed x64 Release development build passed a hands-on zero-vault accessibility pass with the test operator. Confirmed behavior includes immediate `/` shortcut handling at startup; sidebar, Preferences-tab, Learning Center, Add Vault, and new-vault keyboard focus; Narrator control names and Vault Doctor live announcements; VaultKind larger text while Windows Text size was already set to the test operator's normal 160%; minimum-window scrolling and reachability; visible focus in Light and Dark; and Windows High Contrast rendering. Moving the window to a television configured at an unusually large effective scale made navigation substantially different but remained functional, and moving between displays triggered immediate reflow. This is development evidence, not the final release gate. Vault context-menu access, connected-vault and sensitive workflows, broader supported-DPI coverage, and the complete repeat on the frozen release candidate remain outstanding.

On July 31, 2026, the signed x64 Release build passed the new multi-vault navigation checkpoint with zero build warnings or errors and all 68 native checks green. Live inspection reported `VaultKind Java Engine`, the permanent `%LOCALAPPDATA%\VaultKind\engine\settings.json` profile, locked `VK1` at `F:\VK1\VK1`, and locked `VK2` at `F:\Test1\VK2`. The test operator confirmed the prominent Add Vault action, permanent Vault Manager destination, neutral two-vault overview, deliberate VK2 selection, return to the overview, and correct operation. Both authored binaries passed SignTool verification under the local development certificate. This confirms the one/many-vault sidebar and selection flow; final release-candidate assistive-technology testing remains required.

On August 1, 2026, the current signed four-vault checkpoint build reconfirmed the keyboard-only matrix already exercised across the preceding signed accessibility builds. Forward `Tab` and reverse `Shift+Tab` kept visible focus, every vault's adjacent **More actions** button was reachable, `Enter` opened the focused menu, `Escape` dismissed it, and no focus trap occurred. Up/Down traversed and wrapped across the complete sidebar, while Home/End reached its boundaries; focus remained visible throughout. Preferences Left/Right wrapped across all five tabs, and Home/End selected General/About. Learning Center opened with Search focused; Search-to-topic Up/Down boundaries, topic traversal, live filtering, Escape-to-clear, and Escape-to-Dashboard all behaved as documented. In Add Vault, Enter with an empty name did not advance, a valid `Accessibility Test` name advanced to storage selection, and Back exited without creating a vault or folder; focus remained visible and returned sensibly. The test operator confirmed the broader previously completed keyboard workflows continue to work correctly on this build. Because the next frozen commit changes release evidence only, not runtime code or assets, this completes the keyboard-only gate for that unchanged binary.

The following interaction-ten checkpoint rebuilt the exact executable targeted by `VaultKind Development.lnk` with zero warnings and errors, passed all 87 native checks, and produced valid `CN=VaultKind Development` signatures on both authored binaries. The test operator launched the desktop shortcut and confirmed the Dashboard, vault list, and normal functions. Read-only live inspection reported `VaultKind Java Engine`, permanent profile `%LOCALAPPDATA%\VaultKind\engine\settings.json`, Automatic, all five providers, and five locked registrations: `MyMoose` at `F:\MooseTaxes`, `MyDog` at `F:\MooseT`, `Mr Deeds` at `F:\Farm`, `DPCable` at `F:\MooseT\DPCable`, and `KybTest` at `F:\MooseT\KybTest`. This checkpoint validates the unchanged runtime beneath the documentation and Store-media commit.

## Deferred non-Store signed distribution

Directly downloadable signed releases would require an organization-controlled signing identity, timestamping, and separate install, upgrade, uninstall, publisher-identity, and clean-machine testing. The project does not assume that recurring expense will ever be accepted. The current-user development certificate is never an acceptable shortcut for public distribution.

## Current native regression checks

Run:

```powershell
dotnet run --project native\VaultKind.Windows.Tests\VaultKind.Windows.Tests.csproj -c Release
```

The current 87 checks lock down critical Doctor classification, the exact vault-in-use warning boundary, keyboard-navigation boundaries, primary vault destinations, backend identity/profile validation, development/Store package-kind and data-path isolation, vault-creation path resolution, the embedded keyboard guide, local convenience-state persistence, atomic window-placement replacement, Learning Center progress filtering, and Dashboard locked/unlocked counts. This is still an early seam; backend protocol and workflow coverage must continue to grow before 1.0.

## Explicitly excluded from 1.0

- Any multilingual, translation, or runtime language-selection feature.
- A VaultKind updater without a dedicated signed release channel.
- Automatic Vault Doctor repairs.
- macOS, Linux, web, or mobile packages.
